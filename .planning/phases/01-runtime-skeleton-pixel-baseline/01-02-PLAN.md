---
phase: 01-runtime-skeleton-pixel-baseline
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/main.js
  - src/style.css
autonomous: true
requirements: [VIZ-01, VIZ-02]
user_setup: []
must_haves:
  truths:
    - Pixel rendering remains sharp without smoothing artifacts.
    - Foreground gameplay entities remain readable against background and effects.
    - Visual baseline can be regression-checked deterministically.
  artifacts:
    - src/main.js
    - src/style.css
  key_links:
    - renderer and texture configuration enforce nearest-neighbor policy consistently.
    - layering and effect styling preserve silhouette readability for gameplay entities.
---

<objective>
Lock HD pixel baseline and scene readability rules for Phase 1.

Purpose: prevent visual regressions that would invalidate core pixel-art direction.
Output: hardened renderer/texture policy and stable foreground-background readability rules.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-runtime-skeleton-pixel-baseline/01-CONTEXT.md
@.planning/phases/01-runtime-skeleton-pixel-baseline/01-RESEARCH.md
@.planning/phases/01-runtime-skeleton-pixel-baseline/01-01-SUMMARY.md
@src/main.js
@src/style.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enforce pixel clarity rendering policies</name>
  <files>src/main.js</files>
  <behavior>
    - Nearest filtering remains enforced for all generated textures.
    - Renderer pixel ratio cap remains 1.5.
    - Pixel-grid alignment hooks exist for sprite/camera presentation.
  </behavior>
  <action>Audit and harden render and texture paths so nearest-neighbor and mipmap rules are explicit and centralized. Preserve adaptive resolution strategy while preventing accidental smoothing regressions.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Pixel baseline controls are explicit and stable in the runtime rendering path.</done>
</task>

<task type="auto">
  <name>Task 2: Stabilize foreground readability and visual baseline checks</name>
  <files>src/main.js, src/style.css</files>
  <behavior>
    - Effects do not obscure core player/enemy silhouette.
    - Background contrast remains weaker than gameplay entities.
    - A reproducible visual-check procedure is captured for regression use.
  </behavior>
  <action>Refine layering and contrast rules for combat readability and document deterministic screenshot-check steps inside execution summary expectations. Keep weak but useful arena boundary visibility.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Foreground readability and pixel style constraints are enforceable and testable.</done>
</task>

</tasks>

<verification>
- [ ] Build succeeds after render policy changes.
- [ ] VIZ-01 and VIZ-02 map to concrete checks in done criteria.
- [ ] No new post-processing dependency introduced.
</verification>

<success_criteria>
- All tasks completed.
- Pixel quality remains sharp and deterministic.
- Foreground readability is preserved under combat effects.
</success_criteria>

<output>
After completion, create `.planning/phases/01-runtime-skeleton-pixel-baseline/01-02-SUMMARY.md`
</output>
