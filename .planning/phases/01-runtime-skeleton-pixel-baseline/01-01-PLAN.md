---
phase: 01-runtime-skeleton-pixel-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main.js
  - src/style.css
  - index.html
autonomous: true
requirements: [CORE-01, UX-02]
user_setup: []
must_haves:
  truths:
    - Users can enter combat from the start screen in at most two interactions.
    - Start flow remains discoverable through both button and keyboard entry.
    - Pre-combat controls are clearly visible on start screen without extra menus.
  artifacts:
    - src/main.js
    - src/style.css
    - index.html
  key_links:
    - start-screen controls text maps to actual key handlers in runtime input code.
    - start button and keyboard entry both route to startRun through the same transition path.
---

<objective>
Build a reliable runtime shell and onboarding flow for Phase 1.

Purpose: guarantee fast and clear entry into combat while preserving onboarding clarity.
Output: stable start flow behavior, transition timing hooks, and clean hint presentation.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-runtime-skeleton-pixel-baseline/01-CONTEXT.md
@.planning/phases/01-runtime-skeleton-pixel-baseline/01-RESEARCH.md
@src/main.js
@src/style.css
@index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate start-entry behavior and transition gate</name>
  <files>src/main.js</files>
  <behavior>
    - Start button click enters combat.
    - Enter and Space from start mode enter combat.
    - Fullscreen failure does not block start transition.
  </behavior>
  <action>Ensure start-screen interactions route through one transition path and preserve dual entry support (button + keyboard). Keep transition envelope around one second while maintaining deterministic state reset for input buffers.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Start flow is deterministic and discoverable; users can consistently enter combat in at most two interactions.</done>
</task>

<task type="auto">
  <name>Task 2: Harden onboarding hint presentation for start mode</name>
  <files>index.html, src/style.css, src/main.js</files>
  <behavior>
    - Hint content stays minimal and command-style.
    - Hints appear on start screen and do not remain as persistent combat overlay text.
  </behavior>
  <action>Adjust start-screen hint copy and styling to keep high readability and minimal cognitive load. Ensure display logic remains mode-scoped so hints are visible every run before gameplay and do not clutter combat state.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Onboarding hints are concise, visible pre-combat, and tied cleanly to start mode behavior.</done>
</task>

</tasks>

<verification>
- [ ] Build succeeds with no syntax/runtime bundling errors.
- [ ] Start mode supports button and keyboard entry.
- [ ] Hint block is readable and limited to start mode.
</verification>

<success_criteria>
- All tasks completed.
- CORE-01 and UX-02 are explicitly satisfied in behavior.
- No regressions in existing mode transitions.
</success_criteria>

<output>
After completion, create `.planning/phases/01-runtime-skeleton-pixel-baseline/01-01-SUMMARY.md`
</output>
