---
phase: 04-control-state-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main.js
  - src/control-rules.js
  - tests/control-rules.test.js
autonomous: true
requirements: [CORE-04, UX-01, UX-03]
user_setup: []
must_haves:
  truths:
    - Pause/resume keeps deterministic mode transitions and never leaves stale movement/attack input.
    - Focus loss/gain transitions are side-effect safe and do not produce ghost input.
    - Fullscreen toggle/exit failure paths keep controls responsive and mode-safe.
    - render_game_to_text exposes control-state diagnostics required for automation visibility.
  artifacts:
    - src/main.js
    - src/control-rules.js
    - tests/control-rules.test.js
  key_links:
    - input event handlers feed edge/state channels and route transition intent through pure control rules.
    - update loop consumes edge triggers exactly once and applies pause/focus/fullscreen transitions deterministically.
    - render_game_to_text publishes input/pause/fullscreen/focus diagnostics from runtime state.
---

<objective>
Harden control-state transitions for pause/resume, focus lifecycle, and fullscreen toggling in Phase 4.

Purpose: eliminate state corruption and ghost-input behavior while preserving deterministic fixed-step runtime semantics.
Output: pure control-rule layer, guarded transition wiring, and control diagnostics payload for verification.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-control-state-hardening/04-CONTEXT.md
@.planning/phases/04-control-state-hardening/04-RESEARCH.md
@.planning/phases/04-control-state-hardening/04-VALIDATION.md
@src/main.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract deterministic control rules and harden pause/focus transitions</name>
  <files>src/main.js, src/control-rules.js, tests/control-rules.test.js</files>
  <behavior>
    - Pause toggles only on one-shot edge consumption.
    - Pause resumes without stale pressed/edge keys.
    - Focus/visibility loss clears control buffers and applies deterministic pause transition.
    - Recovery metadata is tracked for diagnostics without mutating combat state unexpectedly.
  </behavior>
  <action>Introduce a pure control-rule module for transition decisions and route existing key/focus handlers through it. Replace direct edge-set probing with single-consume helpers so one-shot controls cannot double-trigger.</action>
  <verify>
    <automated>node --test tests/control-rules.test.js</automated>
    <automated>npm run build</automated>
  </verify>
  <done>CORE-04 baseline is satisfied with deterministic transition logic and automated rule-level coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Harden fullscreen fallback and expose control diagnostics for verification</name>
  <files>src/main.js, tests/control-rules.test.js</files>
  <behavior>
    - `KeyF` toggles fullscreen intent safely and `Escape` exits fullscreen without mode/input corruption.
    - Fullscreen API rejection paths are captured as diagnostics instead of mutating gameplay mode.
    - `render_game_to_text` includes `inputState`, `pauseState`, `fullscreenState`, and `focusState` fields.
    - Paused/focus-recovery status remains readable through existing overlay channels.
  </behavior>
  <action>Wrap fullscreen requests in non-throwing adapters, wire result metadata into runtime state, and extend text-render payload with control diagnostics to support UX-01/UX-03 verification and future automation assertions.</action>
  <verify>
    <automated>node --test tests/control-rules.test.js</automated>
    <automated>npm run build</automated>
  </verify>
  <done>UX-01 and UX-03 are verifiable via stable control behavior and diagnostics payload output.</done>
</task>

</tasks>

<verification>
- [ ] Build succeeds.
- [ ] Control transitions consume edge inputs exactly once.
- [ ] Focus/fullscreen paths remain mode-safe and input-safe.
- [ ] render_game_to_text includes required control-state diagnostics fields.
</verification>

<success_criteria>
- All tasks completed.
- CORE-04, UX-01, and UX-03 are explicitly satisfied and testable.
- No regression in deterministic combat/frame update behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/04-control-state-hardening/04-01-SUMMARY.md`
</output>
