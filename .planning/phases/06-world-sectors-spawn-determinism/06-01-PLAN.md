---
phase: 06-world-sectors-spawn-determinism
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/world-sectors.js
  - src/main.js
  - tests/world-sectors.test.js
autonomous: true
requirements: [MAP-01]
user_setup: []
must_haves:
  truths:
    - One run supports seamless traversal across at least three connected sectors with no scene reload.
    - Sector topology and adjacency are defined as a single source of truth consumed by runtime and tests.
    - Traversal state tracks visited sectors deterministically for automation assertions.
  artifacts:
    - src/world-sectors.js
    - src/main.js
    - tests/world-sectors.test.js
  key_links:
    - topology contract drives runtime sector transitions and connectivity assertions.
    - visited sector trace is exported through deterministic render bridge for replay checks.
---

<objective>
Establish the sector topology contract and traversal state for seamless multi-sector movement.

Purpose: satisfy MAP-01 with a deterministic, testable world layout contract.
Output: hub-plus-ring sector model, runtime traversal bookkeeping, and connectivity/traversal tests.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-world-sectors-spawn-determinism/06-CONTEXT.md
@.planning/phases/06-world-sectors-spawn-determinism/06-RESEARCH.md
@.planning/phases/06-world-sectors-spawn-determinism/06-VALIDATION.md
@src/main.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build hub-plus-ring sector topology contract</name>
  <files>src/world-sectors.js, tests/world-sectors.test.js</files>
  <behavior>
    - Define at least one hub sector and at least three connected outer sectors.
    - Encode adjacency, bounds, and traversal lanes in deterministic ordered structures.
    - Export pure helpers for connectivity and sector lookup so tests can assert graph validity.
  </behavior>
  <action>Create `world-sectors` data contract and helper APIs, then add unit tests asserting at-least-three-sector connectivity and deterministic ordering of sector identities.</action>
  <verify>
    <automated>node --test tests/world-sectors.test.js</automated>
  </verify>
  <done>Topology model is machine-verifiable and guarantees MAP-01 connectivity prerequisites.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate traversal state tracking into runtime loop</name>
  <files>src/main.js, tests/world-sectors.test.js</files>
  <behavior>
    - Runtime tracks `currentSectorId`, `visitedSectorIds`, and `transitionSeq` in world state.
    - Crossing lane boundaries updates sector state without scene teardown or blocking transition.
    - Deterministic text bridge includes visited-sector summary for later regression checks.
  </behavior>
  <action>Wire topology helpers into movement update to resolve sector transitions continuously, then extend tests to assert one scripted route visits at least three connected sectors in one run.</action>
  <verify>
    <automated>node --test tests/world-sectors.test.js</automated>
    <automated>npm run build</automated>
  </verify>
  <done>MAP-01 is covered by automated traversal assertions and build-stable runtime integration.</done>
</task>

</tasks>

<verification>
- [ ] `tests/world-sectors.test.js` validates graph connectivity and three-sector traversal sequence.
- [ ] Runtime traversal updates occur with no scene reload behavior.
- [ ] Build succeeds after topology integration.
</verification>

<success_criteria>
- All tasks completed.
- MAP-01 is satisfied with deterministic topology and traversal evidence.
- Traversal state is exposed for downstream phase assertions.
</success_criteria>

<output>
After completion, create `.planning/phases/06-world-sectors-spawn-determinism/06-01-SUMMARY.md`
</output>
