---
phase: 06-world-sectors-spawn-determinism
plan: 04
type: execute
wave: 4
depends_on: [06-02, 06-03]
files_modified:
  - src/main.js
  - src/style.css
  - src/determinism-harness.js
  - tests/playwright-map-sectors.test.js
autonomous: true
requirements: [MAP-04]
user_setup: []
must_haves:
  truths:
    - Safe lanes and choke zones are visually distinguishable during active combat.
    - Readability signals are driven by sector topology contract, not ad-hoc visual-only data.
    - Automated e2e checks validate readability indicators and three-sector traversal continuity.
  artifacts:
    - src/main.js
    - src/style.css
    - src/determinism-harness.js
    - tests/playwright-map-sectors.test.js
  key_links:
    - topology-derived lane/choke metadata feeds ground rendering and boundary emphasis.
    - e2e script asserts readability markers and traversal output through runtime text bridge.
---

<objective>
Deliver combat readability signals for safe lanes and choke zones with automated behavior checks.

Purpose: satisfy MAP-04 while preserving deterministic world and spawn integration from prior plans.
Output: lane/choke visual encoding, minimal HUD extension, and Playwright map-sector verification.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-world-sectors-spawn-determinism/06-CONTEXT.md
@.planning/phases/06-world-sectors-spawn-determinism/06-RESEARCH.md
@.planning/phases/06-world-sectors-spawn-determinism/06-VALIDATION.md
@.planning/phases/06-world-sectors-spawn-determinism/06-02-SUMMARY.md
@.planning/phases/06-world-sectors-spawn-determinism/06-03-SUMMARY.md
@src/main.js
@src/style.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement topology-driven readability signals and minimal HUD indicators</name>
  <files>src/main.js, src/style.css, src/determinism-harness.js</files>
  <behavior>
    - Ground and boundary visuals encode primary lanes vs choke zones based on sector metadata.
    - Boundary-touch emphasis highlights constrained routes without overwhelming gameplay view.
    - HUD adds only minimal `Sector` and `Pressure` indicators with deterministic update behavior.
  </behavior>
  <action>Integrate lane/choke rendering cues from sector contract into world visuals, add compact HUD fields for current sector and pressure level, and expose readability-supporting fields in text snapshot for assertions.</action>
  <verify>
    <automated>node --test tests/determinism-contract.test.js</automated>
    <automated>npm run build</automated>
  </verify>
  <done>Readability cues are live in runtime and minimally observable through deterministic output.</done>
</task>

<task type="auto">
  <name>Task 2: Add Playwright map-sector e2e checks for readability and traversal continuity</name>
  <files>tests/playwright-map-sectors.test.js, src/main.js</files>
  <behavior>
    - Script performs scripted traversal that visits at least three sectors in one run.
    - Script asserts readability indicators exist and update with sector changes.
    - Script fails on critical console/runtime errors and stores deterministic debug artifacts.
  </behavior>
  <action>Create Playwright map-sector test flow that verifies traversal continuity, lane/choke readability indicators, and runtime stability under active combat pressure.</action>
  <verify>
    <automated>node tests/playwright-map-sectors.test.js</automated>
    <automated>npm run build</automated>
  </verify>
  <done>MAP-04 is covered by repeatable e2e evidence tied to topology and runtime output.</done>
</task>

<task type="auto">
  <name>Task 3: Run phase-level deterministic regression gate</name>
  <files>tests/world-sectors.test.js, tests/spawn-director.test.js, tests/determinism-contract.test.js, tests/playwright-map-sectors.test.js</files>
  <behavior>
    - Full Phase 06 verification runs as one deterministic gate.
    - Failures clearly identify whether issue belongs to topology, collision, spawn, or readability layers.
    - Final wave completion evidence is recorded in summary output.
  </behavior>
  <action>Execute the full suite command from `06-VALIDATION.md` after integrating readability and e2e checks to lock MAP-01..MAP-04 regression safety.</action>
  <verify>
    <automated>npm run build && node --test tests/world-sectors.test.js tests/spawn-director.test.js tests/determinism-contract.test.js && node tests/playwright-map-sectors.test.js</automated>
  </verify>
  <done>Phase 06 exits with a full deterministic and e2e verification gate.</done>
</task>

</tasks>

<verification>
- [ ] Readability signals are topology-driven and visible during combat.
- [ ] Playwright map-sector script verifies three-sector traversal plus readability indicators.
- [ ] Full Phase 06 suite passes with deterministic replay checks intact.
</verification>

<success_criteria>
- All tasks completed.
- MAP-04 is satisfied with automated evidence and minimal HUD/readability instrumentation.
- Phase-level full suite command is green before handoff.
</success_criteria>

<output>
After completion, create `.planning/phases/06-world-sectors-spawn-determinism/06-04-SUMMARY.md`
</output>
