---
phase: 06-world-sectors-spawn-determinism
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - src/world-collision.js
  - src/main.js
  - tests/world-sectors.test.js
autonomous: true
requirements: [MAP-02]
user_setup: []
must_haves:
  truths:
    - Player and enemy movement remain collision-stable at sector boundaries and lanes.
    - Boundary resolver behavior is deterministic and independently testable as pure logic.
    - Boundary edge cases (corner slide, rebound, chase crossing) are covered by automated assertions.
  artifacts:
    - src/world-collision.js
    - src/main.js
    - tests/world-sectors.test.js
  key_links:
    - movement integration calls boundary resolver before committing runtime positions.
    - test scenarios replay boundary contact paths for both player and enemy pursuit loops.
---

<objective>
Harden sector-boundary collision behavior for both player traversal and enemy pursuit.

Purpose: satisfy MAP-02 with deterministic collision resolution at sector transitions.
Output: reusable boundary resolver module, runtime integration, and edge-case collision tests.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-world-sectors-spawn-determinism/06-CONTEXT.md
@.planning/phases/06-world-sectors-spawn-determinism/06-RESEARCH.md
@.planning/phases/06-world-sectors-spawn-determinism/06-VALIDATION.md
@.planning/phases/06-world-sectors-spawn-determinism/06-01-SUMMARY.md
@src/main.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement deterministic sector boundary resolver</name>
  <files>src/world-collision.js, tests/world-sectors.test.js</files>
  <behavior>
    - Resolve movement using lane pass-through and non-lane rebound/slide behavior.
    - Handle corner and near-boundary micro-steps without oscillation or tunnel-through.
    - Expose pure APIs for player/enemy movement resolution to support strict unit coverage.
  </behavior>
  <action>Create `world-collision` pure resolver functions and add test vectors for slide, rebound, and clamp fallback sequences at sector boundaries.</action>
  <verify>
    <automated>node --test tests/world-sectors.test.js</automated>
  </verify>
  <done>Boundary resolver behavior is deterministic and machine-verified across edge cases.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate resolver into player and enemy update paths</name>
  <files>src/main.js, tests/world-sectors.test.js</files>
  <behavior>
    - Player movement uses boundary resolver instead of raw global clamp-only behavior.
    - Enemy chase path applies the same boundary contract and remains responsive at transitions.
    - Contact and chase checks use post-resolution positions to avoid one-frame mismatch near boundaries.
  </behavior>
  <action>Wire boundary resolver into runtime movement updates for player and enemies, and extend tests to assert stable pursuit and no jitter-loop regressions across sector transitions.</action>
  <verify>
    <automated>node --test tests/world-sectors.test.js</automated>
    <automated>npm run build</automated>
  </verify>
  <done>MAP-02 is covered with deterministic boundary behavior for movement and pursuit.</done>
</task>

</tasks>

<verification>
- [ ] Boundary edge-case tests pass for slide/rebound/chase scenarios.
- [ ] Player and enemy movement both use the deterministic resolver path.
- [ ] Build succeeds after collision integration.
</verification>

<success_criteria>
- All tasks completed.
- MAP-02 is explicitly validated by automated boundary and pursuit assertions.
- Boundary behavior remains stable under fixed-step updates.
</success_criteria>

<output>
After completion, create `.planning/phases/06-world-sectors-spawn-determinism/06-02-SUMMARY.md`
</output>
