---
phase: 06-world-sectors-spawn-determinism
plan: 03
type: execute
wave: 3
depends_on: [06-01]
files_modified:
  - src/spawn-director.js
  - src/main.js
  - src/determinism-harness.js
  - tests/spawn-director.test.js
  - tests/determinism-contract.test.js
autonomous: true
requirements: [MAP-03]
user_setup: []
must_haves:
  truths:
    - Spawn distribution is computed from sector rules with deterministic weighted selection.
    - Same seed and equivalent timeline produce identical spawn-sector event sequences.
    - Determinism snapshot includes spawn director state, spawn cooldown state, and spawn RNG evidence fields.
  artifacts:
    - src/spawn-director.js
    - src/main.js
    - src/determinism-harness.js
    - tests/spawn-director.test.js
    - tests/determinism-contract.test.js
  key_links:
    - runtime spawn loop delegates sector choice and budgeting to spawn-director pure logic.
    - deterministic harness exposes spawn event sequence and RNG-linked replay fields.
---

<objective>
Implement deterministic sector-based spawn distribution and observable replay state.

Purpose: satisfy MAP-03 by converting spawn logic from global edge randomness to sector-rule scheduling.
Output: spawn director module, runtime wiring, deterministic snapshot extensions, and reproducibility tests.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-world-sectors-spawn-determinism/06-CONTEXT.md
@.planning/phases/06-world-sectors-spawn-determinism/06-RESEARCH.md
@.planning/phases/06-world-sectors-spawn-determinism/06-VALIDATION.md
@.planning/phases/06-world-sectors-spawn-determinism/06-01-SUMMARY.md
@src/main.js
@src/determinism-harness.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build sector-aware deterministic spawn director</name>
  <files>src/spawn-director.js, tests/spawn-director.test.js</files>
  <behavior>
    - Compute sector weights from player sector, heat state, global cap, and per-sector soft cap.
    - Produce deterministic sector picks from supplied RNG value with stable tie-break ordering.
    - Track event sequence and selected sector metadata in explicit director state.
  </behavior>
  <action>Introduce pure spawn director APIs for weight calculation and weighted pick decisions, then add unit tests that assert deterministic outputs for repeated input vectors.</action>
  <verify>
    <automated>node --test tests/spawn-director.test.js</automated>
  </verify>
  <done>Core spawn distribution rules are deterministic and test-backed at module level.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate spawn director and extend deterministic snapshot contract</name>
  <files>src/main.js, src/determinism-harness.js, tests/spawn-director.test.js, tests/determinism-contract.test.js</files>
  <behavior>
    - Runtime spawn path uses sector director state (`eventSeq`, `sectorWeights`, `sectorEnemyCounts`, `lastSpawnSectorId`).
    - Determinism snapshot includes spawn replay fields with stable ordering and `spawnCooldown` observability.
    - Contract tests assert same seed + timeline yields identical spawn-sector sequence.
  </behavior>
  <action>Replace direct spawn-point selection path with spawn-director outputs, add snapshot fields for spawn-state observability (including `spawnCooldown`), and extend determinism contract tests for replay parity across repeated runs.</action>
  <verify>
    <automated>node --test tests/spawn-director.test.js tests/determinism-contract.test.js</automated>
    <automated>npm run build</automated>
  </verify>
  <done>MAP-03 is covered by deterministic spawn sequencing and snapshot-level replay evidence.</done>
</task>

</tasks>

<verification>
- [ ] Spawn director tests pass for weighted distribution and deterministic ordering.
- [ ] Determinism contract tests pass for same-seed spawn event parity.
- [ ] Build succeeds after runtime and snapshot integration.
</verification>

<success_criteria>
- All tasks completed.
- MAP-03 is machine-verifiable through director tests and determinism contract assertions.
- Spawn-state observability is stable for downstream automated regression.
</success_criteria>

<output>
After completion, create `.planning/phases/06-world-sectors-spawn-determinism/06-03-SUMMARY.md`
</output>
