---
phase: 08-breakables-loot-and-equipment
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - src/drop-tables.js
  - src/drop-system.js
  - src/main.js
  - src/determinism-harness.js
  - tests/drop-system.test.js
  - tests/determinism-contract.test.js
autonomous: true
requirements: [LOOT-02]
user_setup: []
must_haves:
  truths:
    - Destroyed breakables resolve equipment drops from weighted deterministic drop tables with stable descriptor fields.
    - Ground drops are stored in stable order with reproducible `id`, `position`, `slot`, `rarity`, and stat values.
    - Snapshot captures `dropRngState` and loot event sequencing separately from spawn randomness.
  artifacts:
    - src/drop-tables.js
    - src/drop-system.js
    - src/main.js
    - src/determinism-harness.js
    - tests/drop-system.test.js
    - tests/determinism-contract.test.js
  key_links:
    - breakable destroy events flow into the drop resolver through a dedicated loot state slice rather than directly mutating equipment slots.
    - drop descriptors and `dropRngState` are serialized through the same snapshot bridge that later Playwright and contract tests will assert.
---

<objective>
Deliver deterministic equipment drops and ground-loot state for destroyed breakables.

Purpose: satisfy `LOOT-02` and establish the replayable loot evidence surface required by `LOOT-05`.
Output: weighted drop tables, pure drop resolver, stable ground drop ordering, and snapshot-visible loot RNG state.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-breakables-loot-and-equipment/08-CONTEXT.md
@.planning/phases/08-breakables-loot-and-equipment/08-RESEARCH.md
@.planning/phases/08-breakables-loot-and-equipment/08-VALIDATION.md
@.planning/phases/08-breakables-loot-and-equipment/08-01-PLAN.md
@src/main.js
@src/determinism-harness.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define weighted deterministic equipment drop tables and resolver primitives</name>
  <files>src/drop-tables.js, src/drop-system.js, tests/drop-system.test.js</files>
  <behavior>
    - Drop tables only produce equipment descriptors, never currency, materials, or consumables.
    - Rarity is limited to `common`, `rare`, and `epic`.
    - Resolver output includes stable descriptor fields: `id`, `slot`, `rarity`, `statKey`, `statValue`, `sourcePropId`.
  </behavior>
  <action>Create pure weighted drop table definitions and resolver helpers that accept breakable destroy events plus a dedicated RNG state, then emit stable equipment descriptors for ground loot without introducing pity, affix, or zone-bias logic.</action>
  <verify>
    <automated>node --test tests/drop-system.test.js</automated>
  </verify>
  <done>`LOOT-02` has a deterministic descriptor-level drop contract bounded to Phase 08 scope.</done>
</task>

<task type="auto">
  <name>Task 2: Persist ground drops and serialize loot RNG state through the deterministic snapshot</name>
  <files>src/drop-system.js, src/main.js, src/determinism-harness.js, tests/drop-system.test.js, tests/determinism-contract.test.js</files>
  <behavior>
    - Destroyed breakables create `state.loot.groundDrops` entries in stable order.
    - `state.loot.dropRngState` and `state.loot.eventSeq` remain independent from spawn RNG state.
    - Snapshot exposes `lootState` fields sufficient to explain identical-seed replay parity.
  </behavior>
  <action>Wire breakable destroy results into a dedicated loot state slice, store ground drops with stable ordering and positions, keep `dropRngState` separate from spawn randomness, and extend `buildDeterministicSnapshot()` so contract tests can assert replayable loot outcomes.</action>
  <verify>
    <automated>node --test tests/drop-system.test.js tests/determinism-contract.test.js</automated>
    <automated>npm run build</automated>
  </verify>
  <done>Phase 08 now has deterministic ground-drop state and snapshot-visible loot randomness ready for equipment handling.</done>
</task>

</tasks>

<verification>
- [ ] Weighted deterministic drop tables only emit equipment descriptors.
- [ ] Ground drops are stored in stable order with reproducible slot, rarity, and stat fields.
- [ ] `dropRngState` and loot sequencing are serialized independently from spawn RNG.
</verification>

<success_criteria>
- All tasks completed.
- `LOOT-02` is satisfied and the first half of `LOOT-05` evidence exists.
- No pity, affix, zone bias, inventory, or economy mechanics are introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/08-breakables-loot-and-equipment/08-02-SUMMARY.md`
</output>
