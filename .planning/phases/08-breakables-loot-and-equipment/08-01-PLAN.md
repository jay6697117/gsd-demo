---
phase: 08-breakables-loot-and-equipment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/breakable-catalog.js
  - src/breakable-system.js
  - src/main.js
  - src/determinism-harness.js
  - tests/breakable-system.test.js
  - tests/determinism-contract.test.js
autonomous: true
requirements: [LOOT-01]
user_setup: []
must_haves:
  truths:
    - At least two authored non-blocking breakable archetypes exist with stable ids, hp values, and layout order.
    - A single player attack can deterministically resolve hits against enemies and breakables in the same combat step.
    - render_game_to_text exposes minimal stable-order `world.breakables` summary fields for later loot verification.
  artifacts:
    - src/breakable-catalog.js
    - src/breakable-system.js
    - src/main.js
    - src/determinism-harness.js
    - tests/breakable-system.test.js
  key_links:
    - doAttack consumes authored breakable layout data through pure breakable helpers rather than ad-hoc scene inspection.
    - breakable lifecycle state feeds both combat resolution and deterministic snapshot summary from the same canonical data.
---

<objective>
Deliver the deterministic breakable content contract and attack-resolution foundation for Phase 08.

Purpose: satisfy `LOOT-01` before drop tables, equipment handling, or compare mode are introduced.
Output: authored non-blocking breakable archetypes, stable lifecycle/query helpers, attack-chain integration, and snapshot-ready breakable summary hooks.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-breakables-loot-and-equipment/08-CONTEXT.md
@.planning/phases/08-breakables-loot-and-equipment/08-RESEARCH.md
@.planning/phases/08-breakables-loot-and-equipment/08-VALIDATION.md
@.planning/phases/07-building-tactical-layer/07-VERIFICATION.md
@src/main.js
@src/determinism-harness.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define deterministic non-blocking breakable catalog and authored presets</name>
  <files>src/breakable-catalog.js, src/breakable-system.js, tests/breakable-system.test.js</files>
  <behavior>
    - At least two breakable archetypes such as `crate` and `cache` exist with fixed hp, shape, and visual tokens.
    - Breakables are authored static props with stable ids and authored order.
    - Breakables do not participate in blocker/funnel/soft-cover collision semantics.
  </behavior>
  <action>Create a pure breakable catalog and layout helpers that define archetype metadata, stable instance ids, authored placement order, and lifecycle defaults (`hp`, `maxHp`, `broken`) without introducing blocking collision or reward economy fields.</action>
  <verify>
    <automated>node --test tests/breakable-system.test.js</automated>
  </verify>
  <done>`LOOT-01` has deterministic breakable content primitives with no leakage into building or economy scope.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate breakable hit resolution into the existing attack chain and snapshot summary</name>
  <files>src/main.js, src/breakable-system.js, src/determinism-harness.js, tests/breakable-system.test.js, tests/determinism-contract.test.js</files>
  <behavior>
    - `doAttack()` can damage enemies and breakables in the same step.
    - Breakable hits resolve in stable `id` order and transition once into `broken` state.
    - Snapshot exposes minimal stable-order `world.breakables` summary fields without serializing render objects.
  </behavior>
  <action>Extend the existing player attack path so it queries breakables from pure helpers, resolves hit/destroy transitions in deterministic order, emits minimal break feedback hooks, and serializes breakable state through `buildDeterministicSnapshot()` for later drop/equipment plans.</action>
  <verify>
    <automated>node --test tests/breakable-system.test.js tests/determinism-contract.test.js</automated>
    <automated>npm run build</automated>
  </verify>
  <done>Phase 08 gains a repeatable breakable damage lifecycle that is visible to both runtime logic and deterministic tests.</done>
</task>

</tasks>

<verification>
- [ ] Two or more non-blocking breakable archetypes exist with stable ids and lifecycle defaults.
- [ ] `doAttack()` can resolve enemy hits and breakable hits in the same deterministic step.
- [ ] Snapshot exposes stable-order `world.breakables` summary fields.
</verification>

<success_criteria>
- All tasks completed.
- `LOOT-01` is satisfied with deterministic content and runtime attack wiring.
- No inventory, currency, material, or blocking-collision semantics are introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/08-breakables-loot-and-equipment/08-01-SUMMARY.md`
</output>
