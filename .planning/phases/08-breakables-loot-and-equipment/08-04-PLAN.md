---
phase: 08-breakables-loot-and-equipment
plan: 04
type: execute
wave: 4
depends_on: [08-03]
files_modified:
  - src/determinism-harness.js
  - src/main.js
  - tests/determinism-contract.test.js
  - tests/playwright-breakables-loot.test.js
autonomous: true
requirements: [LOOT-05]
user_setup: []
must_haves:
  truths:
    - render_game_to_text exposes stable `world.breakables`, `lootState`, and `equipmentState` fields sufficient to explain replay parity.
    - A Playwright route proves breakable destruction, deterministic loot pickup, compare accept/reject handling, and equipment application in a live browser run.
    - Full regression confirms the loot loop does not break Phase 06 traversal/spawn guarantees or Phase 07 building tactics/readability guarantees.
  artifacts:
    - src/determinism-harness.js
    - src/main.js
    - tests/determinism-contract.test.js
    - tests/playwright-breakables-loot.test.js
  key_links:
    - runtime breakable, loot, and equipment state are serialized through the same deterministic snapshot bridge used by prior phases.
    - contract and Playwright evidence together prove identical seed plus identical input timeline reproduces identical loot outcomes end-to-end.
---

<objective>
Close Phase 08 with deterministic snapshot coverage, end-to-end loot-route verification, and full regression safety.

Purpose: satisfy `LOOT-05` and produce goal-backward evidence for final phase verification.
Output: stable breakable/loot/equipment snapshot fields, Playwright breakable-loot-equip route, and a regression gate spanning Phases 06 through 08.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-breakables-loot-and-equipment/08-CONTEXT.md
@.planning/phases/08-breakables-loot-and-equipment/08-RESEARCH.md
@.planning/phases/08-breakables-loot-and-equipment/08-VALIDATION.md
@.planning/phases/08-breakables-loot-and-equipment/08-03-PLAN.md
@.planning/phases/07-building-tactical-layer/07-VERIFICATION.md
@.planning/phases/06-world-sectors-spawn-determinism/06-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend deterministic snapshot contract for breakables, loot, and equipment</name>
  <files>src/determinism-harness.js, src/main.js, tests/determinism-contract.test.js</files>
  <behavior>
    - Snapshot exposes stable-order `world.breakables`, `lootState`, and `equipmentState` fields.
    - `dropRngState`, `pendingPickupId`, compare candidate data, and equipped slot summaries are observable without scene-object inspection.
    - Same seed plus same input timeline yields identical loot/equipment snapshot output.
  </behavior>
  <action>Finalize `buildDeterministicSnapshot()` coverage for breakable, loot, and equipment slices, normalize ordering and field names, and extend contract tests so replay parity failures can be diagnosed from text output alone.</action>
  <verify>
    <automated>node --test tests/determinism-contract.test.js</automated>
    <automated>npm run build</automated>
  </verify>
  <done>`LOOT-05` has machine-readable snapshot evidence rather than relying on visual inspection.</done>
</task>

<task type="auto">
  <name>Task 2: Add Playwright route and full regression gate for the complete loot loop</name>
  <files>tests/playwright-breakables-loot.test.js, src/main.js, tests/determinism-contract.test.js</files>
  <behavior>
    - Browser route breaks at least one prop, triggers auto pickup, exercises compare accept or reject, and confirms resulting equipment state.
    - Test artifacts are useful for debugging but the assertions rely on deterministic text state rather than screenshots alone.
    - Full suite preserves Phase 06 traversal/spawn/readability and Phase 07 building tactics behavior.
  </behavior>
  <action>Create a Playwright scenario for breakable destruction and loot handling, then run the full regression command from `08-VALIDATION.md` so the new loot loop proves replay parity without regressing world or building contracts.</action>
  <verify>
    <automated>npm run build && node --test tests/world-sectors.test.js tests/building-system.test.js tests/building-tactics.test.js tests/building-steering.test.js tests/breakable-system.test.js tests/drop-system.test.js tests/equipment-system.test.js tests/determinism-contract.test.js && node tests/playwright-breakables-loot.test.js</automated>
  </verify>
  <done>Phase 08 exits with deterministic and end-to-end verification coverage strong enough for `08-VERIFICATION.md`.</done>
</task>

</tasks>

<verification>
- [ ] Snapshot includes stable breakable, loot, and equipment summary fields.
- [ ] Playwright route proves breakable -> loot -> equip behavior in a live browser run.
- [ ] Full regression confirms no regression against Phase 06 or Phase 07 guarantees.
</verification>

<success_criteria>
- All tasks completed.
- `LOOT-05` is satisfied with deterministic contract and e2e evidence.
- Phase 08 is ready for execution without hidden decisions or unverifiable behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/08-breakables-loot-and-equipment/08-04-SUMMARY.md`
</output>
