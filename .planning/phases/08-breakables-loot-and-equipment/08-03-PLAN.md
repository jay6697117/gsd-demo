---
phase: 08-breakables-loot-and-equipment
plan: 03
type: execute
wave: 3
depends_on: [08-02]
files_modified:
  - src/equipment-system.js
  - src/main.js
  - src/style.css
  - src/determinism-harness.js
  - tests/equipment-system.test.js
  - tests/determinism-contract.test.js
autonomous: true
requirements: [LOOT-03, LOOT-04]
user_setup: []
must_haves:
  truths:
    - Ground loot within pickup radius auto-equips into empty slots or enters a deterministic compare flow for occupied slots.
    - `equip_compare` freezes combat, spawn, and enemy updates until the player accepts or rejects the candidate item.
    - Accept and reject paths both produce stable stat deltas and leave/re-enter re-arm behavior for rejected drops.
  artifacts:
    - src/equipment-system.js
    - src/main.js
    - src/style.css
    - src/determinism-harness.js
    - tests/equipment-system.test.js
  key_links:
    - loot state feeds equipment slot resolution through a dedicated compare-mode state machine rather than direct DOM callbacks.
    - accepted equipment immediately updates derived player stats and snapshot-visible equipment state from the same canonical slot data.
---

<objective>
Deliver auto pickup, slot-based equipment application, and deterministic compare-mode replacement handling.

Purpose: satisfy `LOOT-03` and `LOOT-04` without expanding into inventory or broader progression systems.
Output: pickup radius logic, equipment slot state, compare overlay mode, accept/reject handling, and derived stat rewrites for player combat attributes.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-breakables-loot-and-equipment/08-CONTEXT.md
@.planning/phases/08-breakables-loot-and-equipment/08-RESEARCH.md
@.planning/phases/08-breakables-loot-and-equipment/08-VALIDATION.md
@.planning/phases/08-breakables-loot-and-equipment/08-02-PLAN.md
@src/main.js
@src/style.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add slot-based equipment state and auto-pickup handling</name>
  <files>src/equipment-system.js, src/main.js, tests/equipment-system.test.js</files>
  <behavior>
    - Pickup within radius auto-equips items into empty `weapon`, `core`, or `charm` slots.
    - Pickup logic records `pendingPickupId` when an occupied slot needs comparison.
    - Equipment state exposes stable `slots` and `derivedStats` summaries.
  </behavior>
  <action>Create pure equipment helpers for slot lookup, direct equip, and derived stat aggregation, then wire automatic pickup checks from `state.loot.groundDrops` into runtime state so empty-slot items equip immediately without new input keys.</action>
  <verify>
    <automated>node --test tests/equipment-system.test.js</automated>
  </verify>
  <done>`LOOT-03` has deterministic slot ownership and automatic pickup semantics for empty-slot equipment.</done>
</task>

<task type="auto">
  <name>Task 2: Implement deterministic compare mode, reject re-entry rules, and stat-delta feedback</name>
  <files>src/equipment-system.js, src/main.js, src/style.css, src/determinism-harness.js, tests/equipment-system.test.js, tests/determinism-contract.test.js</files>
  <behavior>
    - Occupied-slot pickup enters `state.mode = "equip_compare"` and pauses combat, spawning, and enemy updates.
    - `Enter` / `Space` accept the new item; `Escape` keeps the old item.
    - Rejected items stay on the ground but only retrigger after the player leaves and re-enters pickup range.
    - Compare UI exposes old/new stat deltas and accepted items immediately rewrite `attackDamage`, `maxHp`, or `moveSpeed`.
  </behavior>
  <action>Add compare-mode state transitions, accept/reject handlers, re-entry arming for rejected drops, minimal overlay/HUD stat-delta feedback, and immediate derived-stat application to player runtime attributes while serializing compare-relevant state through the deterministic snapshot.</action>
  <verify>
    <automated>node --test tests/equipment-system.test.js tests/determinism-contract.test.js</automated>
    <automated>npm run build</automated>
  </verify>
  <done>`LOOT-04` is satisfied with a deterministic compare flow that neither repeats every frame nor leaks combat progression while decisions are pending.</done>
</task>

</tasks>

<verification>
- [ ] Empty-slot pickups auto-equip into `weapon`, `core`, or `charm`.
- [ ] Occupied-slot pickups enter `equip_compare` and freeze combat progression.
- [ ] Accept/reject branches produce clear stat deltas and correct re-entry behavior for rejected items.
</verification>

<success_criteria>
- All tasks completed.
- `LOOT-03` and `LOOT-04` are satisfied without adding inventory or advanced itemization scope.
- Derived equipment stats immediately affect player combat attributes.
</success_criteria>

<output>
After completion, create `.planning/phases/08-breakables-loot-and-equipment/08-03-SUMMARY.md`
</output>
