---
phase: 11-automation-determinism-hardening
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - tests/helpers/playwright-game.js
  - tests/playwright-determinism-replay.test.js
autonomous: true
requirements: [AUTO-05]
user_setup: []
must_haves:
  truths:
    - A deterministic scripted timeline can be replayed across two fresh browser sessions with identical final state.
    - Replay evidence spans sector traversal, loot/equipment resolution, and level-up choice flow.
    - Replay normalization does not discard gameplay-relevant fields.
  artifacts:
    - tests/helpers/playwright-game.js
    - tests/playwright-determinism-replay.test.js
  key_links:
    - `window.advanceTime(ms)` drives the same cross-pipeline outcome when seed and input timeline are identical.
    - dual-session replay parity is proven through normalized snapshot comparison rather than same-session re-read shortcuts.
---

<objective>
Add cross-pipeline deterministic replay verification for v1.1.

Purpose: satisfy `AUTO-05` with a route that proves replay parity across traversal, loot, and level-up flows.
Output: a dual-session replay test and helper support for scripted timelines.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-automation-determinism-hardening/11-CONTEXT.md
@.planning/phases/11-automation-determinism-hardening/11-RESEARCH.md
@.planning/phases/11-automation-determinism-hardening/11-VALIDATION.md
@.planning/phases/11-automation-determinism-hardening/11-01-PLAN.md
@tests/playwright-breakables-loot.test.js
@tests/playwright-progression-levels.test.js
@tests/playwright-levelup-choice.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the shared helper with scripted timeline and dual-session replay support</name>
  <files>tests/helpers/playwright-game.js</files>
  <behavior>
    - Helper can run deterministic scripted input timelines.
    - Helper can execute the same timeline in separate fresh browser sessions.
    - Artifact writing and console gating remain centralized.
  </behavior>
  <action>Upgrade the shared Playwright harness so later replay and route tests can express deterministic input sequences without re-implementing lifecycle boilerplate.</action>
  <verify>
    <automated>node --input-type=module -e "import('./tests/helpers/playwright-game.js')"</automated>
  </verify>
  <done>The helper can drive deterministic browser timelines across multiple fresh sessions.</done>
</task>

<task type="auto">
  <name>Task 2: Prove replay parity across traversal, loot, and level-up pipelines</name>
  <files>tests/playwright-determinism-replay.test.js, tests/helpers/playwright-game.js</files>
  <behavior>
    - Route traverses sectors, resolves one loot/equipment interaction, and resolves one level-up choice path.
    - The same seed and scripted timeline are replayed in run A and run B.
    - Final normalized snapshots are deeply equal.
  </behavior>
  <action>Create a dedicated replay regression script that demonstrates `window.advanceTime(ms)` remains deterministic across the core v1.1 gameplay pipelines.</action>
  <verify>
    <automated>node tests/playwright-determinism-replay.test.js</automated>
    <automated>npm run build</automated>
  </verify>
  <done>`AUTO-05` is satisfied through dual-session cross-pipeline replay evidence.</done>
</task>

</tasks>

<verification>
- [ ] Shared helper supports deterministic scripted timelines.
- [ ] Replay route spans traversal, loot/equipment, and level-up choice.
- [ ] Run A and run B produce identical normalized final snapshots.
- [ ] No gameplay-relevant fields are stripped from replay comparison.
</verification>

<success_criteria>
- All tasks completed.
- `AUTO-05` is satisfied.
- No gameplay systems are refactored beyond what is needed to observe deterministic replay.
</success_criteria>

<output>
After completion, create `.planning/phases/11-automation-determinism-hardening/11-02-SUMMARY.md`
</output>
