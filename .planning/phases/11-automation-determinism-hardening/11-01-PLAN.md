---
phase: 11-automation-determinism-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/determinism-harness.js
  - tests/determinism-contract.test.js
  - tests/helpers/playwright-game.js
autonomous: true
requirements: [AUTO-04]
user_setup: []
must_haves:
  truths:
    - The v1.1 snapshot contract is explicit, schema-versioned, and includes top-level RNG evidence.
    - Existing world, loot, equipment, progression, level-up, upgrade, and spawn sections remain stable and machine-assertable.
    - Shared Playwright helper foundations exist before replay and route hardening begins.
  artifacts:
    - src/determinism-harness.js
    - tests/determinism-contract.test.js
    - tests/helpers/playwright-game.js
  key_links:
    - runtime state is serialized through one canonical snapshot bridge with explicit v1.1 assertion fields.
    - browser regression scripts can reuse one mechanical harness instead of copy-pasting server/browser boilerplate.
---

<objective>
Freeze the v1.1 snapshot contract and introduce the shared Playwright helper skeleton.

Purpose: satisfy `AUTO-04` before replay parity and route hardening build on top of it.
Output: explicit `rngState`, schema bump to `1.4.0`, field-level contract assertions, and a reusable browser-test harness skeleton.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-automation-determinism-hardening/11-CONTEXT.md
@.planning/phases/11-automation-determinism-hardening/11-RESEARCH.md
@.planning/phases/11-automation-determinism-hardening/11-VALIDATION.md
@src/determinism-harness.js
@tests/determinism-contract.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Freeze the v1.1 snapshot contract and add explicit top-level RNG state</name>
  <files>src/determinism-harness.js, tests/determinism-contract.test.js</files>
  <behavior>
    - Existing snapshot sections are preserved and not reduced.
    - `schemaVersion` is bumped to `1.4.0`.
    - A top-level `rngState` exposes `runSeed`, `spawnRngState`, `dropRngState`, and `offerRngState`.
    - Contract tests assert fields by name and expected shape rather than only checking section presence.
  </behavior>
  <action>Extend the deterministic snapshot bridge to freeze the full v1.1 assertion surface, including explicit RNG evidence, and update the contract test to prove field-level stability.</action>
  <verify>
    <automated>node --test tests/determinism-contract.test.js</automated>
  </verify>
  <done>`AUTO-04` has an explicit and versioned snapshot contract.</done>
</task>

<task type="auto">
  <name>Task 2: Introduce a thin shared Playwright helper skeleton for browser regression scripts</name>
  <files>tests/helpers/playwright-game.js</files>
  <behavior>
    - Helper centralizes server startup, browser lifecycle, snapshot reads, artifact writes, and critical console filtering.
    - Helper does not absorb route-specific business logic yet.
    - Helper is syntactically loadable and ready for later waves to consume.
  </behavior>
  <action>Create a reusable browser-test harness module that removes repeated mechanical boilerplate while preserving route ownership inside the existing phase-specific scripts.</action>
  <verify>
    <automated>node --input-type=module -e "import('./tests/helpers/playwright-game.js')"</automated>
    <automated>npm run build</automated>
  </verify>
  <done>The shared Playwright automation foundation exists without prematurely collapsing existing route tests.</done>
</task>

</tasks>

<verification>
- [ ] `schemaVersion` is `1.4.0`.
- [ ] `rngState` exposes the required run-local RNG fields.
- [ ] Existing snapshot sections remain stable and field-level asserted.
- [ ] A reusable Playwright helper skeleton exists and loads cleanly.
</verification>

<success_criteria>
- All tasks completed.
- `AUTO-04` is satisfied.
- No gameplay behavior or stepping semantics change in this wave.
</success_criteria>

<output>
After completion, create `.planning/phases/11-automation-determinism-hardening/11-01-SUMMARY.md`
</output>
