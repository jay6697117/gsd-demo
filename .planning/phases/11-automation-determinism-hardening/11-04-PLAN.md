---
phase: 11-automation-determinism-hardening
plan: 04
type: execute
wave: 4
depends_on: [11-03]
files_modified:
  - tests/helpers/playwright-game.js
  - tests/playwright-restart-parity.test.js
  - package.json
autonomous: true
requirements: [AUTO-08]
user_setup: []
must_haves:
  truths:
    - Restart parity is proven through one dedicated route that covers equipment, loot, progression, level-up, and upgrade reset together.
    - Stable v1.1 automation entrypoints exist in `package.json` for E2E and full regression use.
    - Phase 11 verification can rely on named regression commands instead of ad-hoc shell strings only.
  artifacts:
    - tests/playwright-restart-parity.test.js
    - tests/helpers/playwright-game.js
    - package.json
  key_links:
    - the real restart flow returns all run-local equipment and progression state to baseline.
    - v1.1 browser and regression evidence can be invoked through stable script entrypoints.
---

<objective>
Add dedicated restart parity coverage and consolidate stable v1.1 regression commands.

Purpose: satisfy `AUTO-08` and prepare the final automation evidence surface for `11-VERIFICATION.md`.
Output: a restart parity route plus named `package.json` commands for v1.1 E2E and regression.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-automation-determinism-hardening/11-CONTEXT.md
@.planning/phases/11-automation-determinism-hardening/11-RESEARCH.md
@.planning/phases/11-automation-determinism-hardening/11-VALIDATION.md
@.planning/phases/11-automation-determinism-hardening/11-03-PLAN.md
@tests/playwright-progression-levels.test.js
@tests/playwright-breakables-loot.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prove restart parity through one dedicated cross-system browser route</name>
  <files>tests/helpers/playwright-game.js, tests/playwright-restart-parity.test.js</files>
  <behavior>
    - Route obtains at least one equipment-state change and at least one progression/level-up/upgrade-state change before restart.
    - Route triggers the real restart flow.
    - Post-restart assertions verify `equipmentState`, `lootState`, `progressionState`, `levelUpState`, and `upgradeState` all return to baseline.
  </behavior>
  <action>Create a dedicated browser regression that proves the real restart path resets all relevant run-local combat-growth state together.</action>
  <verify>
    <automated>node tests/playwright-restart-parity.test.js</automated>
  </verify>
  <done>`AUTO-08` is satisfied with one explicit restart parity route.</done>
</task>

<task type="auto">
  <name>Task 2: Consolidate stable v1.1 automation entrypoints in `package.json`</name>
  <files>package.json</files>
  <behavior>
    - `test:e2e:v11` runs the browser routes needed for the v1.1 milestone.
    - `test:regression:v11` runs build, contract tests, and all required browser routes.
    - Commands are stable enough for verifiers and future phases to reuse directly.
  </behavior>
  <action>Add named v1.1 automation scripts so full regression no longer depends on copying long one-off shell strings from planning docs.</action>
  <verify>
    <automated>npm run test:e2e:v11</automated>
    <automated>npm run test:regression:v11</automated>
  </verify>
  <done>The Phase 11 verification surface has stable reusable command entrypoints.</done>
</task>

</tasks>

<verification>
- [ ] Restart parity route proves reset of equipment, loot, progression, level-up, and upgrade state together.
- [ ] `test:e2e:v11` exists and runs the v1.1 browser routes.
- [ ] `test:regression:v11` exists and runs the full v1.1 regression gate.
- [ ] Phase 11 automation evidence is callable through stable named scripts.
</verification>

<success_criteria>
- All tasks completed.
- `AUTO-08` is satisfied.
- v1.1 automation evidence is centralized without changing gameplay behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/11-automation-determinism-hardening/11-04-SUMMARY.md`
</output>
