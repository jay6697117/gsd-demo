---
phase: 07-building-tactical-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/building-catalog.js
  - src/building-system.js
  - src/determinism-harness.js
  - tests/building-system.test.js
autonomous: true
requirements: [BLD-01]
user_setup: []
must_haves:
  truths:
    - Three tactical building archetypes exist as deterministic catalog entries with stable geometry and role metadata.
    - Sector-authored layout presets instantiate buildings in stable order and expose collider plus spawn-exclusion queries.
    - render_game_to_text exposes minimal building summary fields without depending on scene-object serialization.
  artifacts:
    - src/building-catalog.js
    - src/building-system.js
    - src/determinism-harness.js
    - tests/building-system.test.js
  key_links:
    - building catalog feeds layout presets and runtime building summary.
    - spawn-exclusion queries derive from the same collider data used by later movement and steering integration.
---

<objective>
Deliver the deterministic building data contract and layout-query foundation for Phase 07.

Purpose: satisfy the structural half of `BLD-01` before runtime integration or enemy steering begins.
Output: archetype catalog, sector-authored layout presets, stable query APIs, and snapshot-ready building summary hooks.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-building-tactical-layer/07-RESEARCH.md
@.planning/phases/07-building-tactical-layer/07-VALIDATION.md
@.planning/phases/06-world-sectors-spawn-determinism/06-VERIFICATION.md
@src/world-sectors.js
@src/main.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define deterministic tactical building catalog and sector presets</name>
  <files>src/building-catalog.js, src/building-system.js, tests/building-system.test.js</files>
  <behavior>
    - `blocker`, `funnel`, and `soft-cover` exist as explicit archetypes with fixed role metadata.
    - Each sector has fixed building presets authored in stable order.
    - Layout data does not contain rewards, drop hooks, or destructibility fields.
  </behavior>
  <action>Create a pure building catalog plus sector-authored layout presets that define ids, sector membership, stable shape/bounds, role tags, and minimal visual tokens for the three tactical archetypes.</action>
  <verify>
    <automated>node --test tests/building-system.test.js</automated>
  </verify>
  <done>`BLD-01` has a deterministic content contract with no Phase 08+ scope leakage.</done>
</task>

<task type="auto">
  <name>Task 2: Expose collider, query, spawn-exclusion, and snapshot summary APIs</name>
  <files>src/building-system.js, src/determinism-harness.js, tests/building-system.test.js, tests/determinism-contract.test.js</files>
  <behavior>
    - Runtime consumers can query buildings by sector, collider footprint, and spawn-safe candidate positions.
    - Snapshot output can serialize building summary fields in stable order.
    - Query APIs remain pure and independent from Three.js scene objects.
  </behavior>
  <action>Add pure helpers such as sector building lookup, collider extraction, point-in-building checks, and spawn candidate rejection, then extend the deterministic snapshot contract with minimal building summary placeholders.</action>
  <verify>
    <automated>node --test tests/building-system.test.js tests/determinism-contract.test.js</automated>
    <automated>npm run build</automated>
  </verify>
  <done>Wave 1 finishes with stable building/query primitives that later plans can consume without redefining geometry rules.</done>
</task>

</tasks>

<verification>
- [ ] Three archetypes are defined with deterministic role and geometry metadata.
- [ ] Building presets and query APIs are stable-order and sector-scoped.
- [ ] Snapshot contract can expose minimal building summary fields.
</verification>

<success_criteria>
- All tasks completed.
- `BLD-01` foundation is present and fully testable.
- No reward, drop, or destructibility fields are introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/07-building-tactical-layer/07-01-SUMMARY.md`
</output>
