---
phase: 07-building-tactical-layer
plan: 04
type: execute
wave: 3
depends_on: [07-02, 07-03]
files_modified:
  - src/determinism-harness.js
  - tests/determinism-contract.test.js
  - tests/playwright-building-tactics.test.js
  - src/main.js
autonomous: true
requirements: [BLD-04]
user_setup: []
must_haves:
  truths:
    - render_game_to_text exposes stable `world.buildings` and `world.tactics` fields for tactical building assertions.
    - A Playwright route verifies three-sector traversal plus tactical building use under live combat pressure.
    - Full regression proves buildings do not break Phase 06 traversal, collision, spawn ordering, or readability contracts.
  artifacts:
    - src/determinism-harness.js
    - tests/determinism-contract.test.js
    - tests/playwright-building-tactics.test.js
    - src/main.js
  key_links:
    - runtime building/tactics state is serialized through the same deterministic snapshot bridge used by prior phases.
    - end-to-end tactical route evidence and full-suite regression protect both Phase 06 and Phase 07 guarantees.
---

<objective>
Close Phase 07 with deterministic snapshot coverage, tactical e2e verification, and full regression safety.

Purpose: satisfy `BLD-04` and produce goal-backward evidence for final phase verification.
Output: stable building/tactics snapshot fields, Playwright tactical route artifacts, and a full regression gate spanning Phases 06 and 07.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-building-tactical-layer/07-RESEARCH.md
@.planning/phases/07-building-tactical-layer/07-VALIDATION.md
@.planning/phases/07-building-tactical-layer/07-02-PLAN.md
@.planning/phases/07-building-tactical-layer/07-03-PLAN.md
@.planning/phases/06-world-sectors-spawn-determinism/06-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend deterministic snapshot contract for buildings and tactical summaries</name>
  <files>src/determinism-harness.js, tests/determinism-contract.test.js, src/main.js</files>
  <behavior>
    - Snapshot exposes stable-order building and tactics summaries.
    - New fields are derived from runtime state, not scene-object inspection.
    - Same seed plus same input timeline yields identical building/tactics snapshot output.
  </behavior>
  <action>Serialize minimal `world.buildings` and `world.tactics` data through `buildDeterministicSnapshot()` and extend the contract tests to assert stable structure, ordering, and replay parity.</action>
  <verify>
    <automated>node --test tests/determinism-contract.test.js</automated>
    <automated>npm run build</automated>
  </verify>
  <done>`BLD-04` has machine-readable tactical evidence suitable for later phase verification.</done>
</task>

<task type="auto">
  <name>Task 2: Add Playwright tactical route coverage for traversal plus building use</name>
  <files>tests/playwright-building-tactics.test.js, src/main.js</files>
  <behavior>
    - Script visits at least three sectors in one run while using at least one tactical building pattern.
    - Script asserts tactical HUD/snapshot markers and stores artifacts for debugging.
    - Script tolerates known-safe headless WebGL fallback but fails on real runtime errors.
  </behavior>
  <action>Create a Playwright tactical route that proves the player can traverse sectors, exploit a building tactic, and preserve runtime stability under active combat pressure.</action>
  <verify>
    <automated>node tests/playwright-building-tactics.test.js</automated>
    <automated>npm run build</automated>
  </verify>
  <done>Phase 07 gains repeatable end-to-end tactical evidence instead of manual-only demonstrations.</done>
</task>

<task type="auto">
  <name>Task 3: Run full Phase 07 regression gate without regressing Phase 06 guarantees</name>
  <files>tests/world-sectors.test.js, tests/building-system.test.js, tests/building-tactics.test.js, tests/building-steering.test.js, tests/determinism-contract.test.js, tests/playwright-building-tactics.test.js</files>
  <behavior>
    - Full suite validates tactical buildings without regressing traversal, collision, spawn, or readability.
    - Failures isolate whether the breakage belongs to catalog/query, player tactics, steering, snapshot, or e2e wiring.
    - Final must_haves map directly into `07-VERIFICATION.md` goal-backward checks.
  </behavior>
  <action>Execute the full suite command from `07-VALIDATION.md`, confirm all BLD requirements are covered once, and lock the final evidence surface for phase verification.</action>
  <verify>
    <automated>npm run build && node --test tests/world-sectors.test.js tests/building-system.test.js tests/building-tactics.test.js tests/building-steering.test.js tests/determinism-contract.test.js && node tests/playwright-building-tactics.test.js</automated>
  </verify>
  <done>Phase 07 exits with deterministic and e2e verification coverage strong enough for `07-VERIFICATION.md`.</done>
</task>

</tasks>

<verification>
- [ ] Snapshot includes stable building and tactical summary fields.
- [ ] Playwright route proves three-sector traversal plus tactical building usage.
- [ ] Full suite confirms no regression against Phase 06 guarantees.
</verification>

<success_criteria>
- All tasks completed.
- `BLD-04` is satisfied with deterministic contract and end-to-end evidence.
- Phase 07 is ready for execution without hidden decisions.
</success_criteria>

<output>
After completion, create `.planning/phases/07-building-tactical-layer/07-04-SUMMARY.md`
</output>
