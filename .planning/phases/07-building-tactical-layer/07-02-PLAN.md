---
phase: 07-building-tactical-layer
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - src/main.js
  - src/world-collision.js
  - src/style.css
  - tests/building-tactics.test.js
autonomous: true
requirements: [BLD-02]
user_setup: []
must_haves:
  truths:
    - Tactical buildings appear in runtime scenes with stable sector ownership and minimal tactical cues.
    - Player movement collides deterministically with building colliders while preserving Phase 06 sector traversal rules.
    - Reproducible player routes can realize `line-break`, `kite pivot`, and `retreat pocket` using the three archetypes.
  artifacts:
    - src/main.js
    - src/world-collision.js
    - src/style.css
    - tests/building-tactics.test.js
  key_links:
    - runtime scene building sync reads the same presets emitted by the building system.
    - player collision and tactical summaries share one building collider source of truth.
---

<objective>
Integrate tactical buildings into runtime rendering, player movement, and minimal tactical feedback.

Purpose: satisfy `BLD-02` by making the tactical building foundation usable in runtime without expanding into rewards or new interaction systems.
Output: scene-visible buildings, building-aware player collision, tactical world summaries, and repeatable player route evidence.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-building-tactical-layer/07-RESEARCH.md
@.planning/phases/07-building-tactical-layer/07-VALIDATION.md
@.planning/phases/07-building-tactical-layer/07-01-PLAN.md
@src/main.js
@src/world-collision.js
@src/style.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sync sector buildings into runtime state, scene, and tactical summaries</name>
  <files>src/main.js, src/style.css, src/determinism-harness.js</files>
  <behavior>
    - Runtime state tracks `world.buildings` and `world.tactics` in stable order.
    - Buildings render with minimal but readable role cues using the existing visual language.
    - HUD/readability only gains minimal tactical additions and does not create a new overlay system.
  </behavior>
  <action>Instantiate sector-authored buildings into the scene, maintain building/tactics state slices, and expose minimal tactical hints that tell the player which structures are blockers, funnels, or soft-cover pockets.</action>
  <verify>
    <automated>node --test tests/building-system.test.js tests/determinism-contract.test.js</automated>
  </verify>
  <done>Runtime state and visuals can express building presence and tactical role without new interaction UI.</done>
</task>

<task type="auto">
  <name>Task 2: Extend player movement with building-aware collision and tactical route assertions</name>
  <files>src/world-collision.js, src/main.js, tests/building-tactics.test.js</files>
  <behavior>
    - Player movement respects building colliders with deterministic slide/rebound behavior.
    - Existing sector traversal and lane crossing semantics remain intact.
    - Automated routes prove `blocker` line-break, `funnel` kite pivot, and `soft-cover` retreat pocket outcomes.
  </behavior>
  <action>Add building collider handling to the existing movement resolver and create deterministic player-route tests that demonstrate each archetype lowers immediate pressure through movement alone.</action>
  <verify>
    <automated>node --test tests/building-system.test.js tests/building-tactics.test.js</automated>
    <automated>npm run build</automated>
  </verify>
  <done>`BLD-02` is backed by deterministic movement behavior instead of purely visual intent.</done>
</task>

</tasks>

<verification>
- [ ] Buildings render and serialize with stable sector ownership.
- [ ] Player collision remains deterministic and traversal-safe around buildings.
- [ ] Automated routes prove blocker / funnel / soft-cover tactical use.
</verification>

<success_criteria>
- All tasks completed.
- Runtime tactical building behavior is visible and exploitable with existing movement controls.
- No new interaction key, reward, or destructibility behavior is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/07-building-tactical-layer/07-02-SUMMARY.md`
</output>
