---
phase: 07-building-tactical-layer
plan: 03
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - src/building-system.js
  - src/main.js
  - tests/building-steering.test.js
autonomous: true
requirements: [BLD-03]
user_setup: []
must_haves:
  truths:
    - Enemy obstacle steering uses deterministic candidate selection and explicit unstuck fallback instead of random detours or heavy pathfinding.
    - Stress pursuit near blockers, funnels, and soft-cover pockets avoids persistent stuck loops and mesh penetration.
    - Spawn-near-building and sector-transition chase cases remain stable under fixed-step replay.
  artifacts:
    - src/building-system.js
    - src/main.js
    - tests/building-steering.test.js
  key_links:
    - enemy desired velocity selection consumes the same building collider queries used by player collision and spawn exclusion.
    - unstuck fallback is observable through deterministic tests and later snapshot evidence.
---

<objective>
Add deterministic enemy steering and unstuck behavior around tactical buildings.

Purpose: satisfy `BLD-03` without introducing navmesh, asymmetrical traversal, or unstable random detours.
Output: pure steering helpers, runtime enemy integration, and stress-route coverage for blocked chases.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-building-tactical-layer/07-RESEARCH.md
@.planning/phases/07-building-tactical-layer/07-VALIDATION.md
@.planning/phases/07-building-tactical-layer/07-01-PLAN.md
@src/building-system.js
@src/main.js
@src/world-collision.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement pure building-aware steering and unstuck helpers</name>
  <files>src/building-system.js, tests/building-steering.test.js</files>
  <behavior>
    - Steering chooses from a fixed ordered set of candidate directions around building colliders.
    - Unstuck fallback has explicit trigger thresholds and deterministic escape behavior.
    - No A*, navmesh, or extra RNG stream is introduced.
  </behavior>
  <action>Create pure helpers that transform a target pursuit vector plus building collider queries into a deterministic movement recommendation and an explicit unstuck fallback when enemies remain blocked for too many fixed steps.</action>
  <verify>
    <automated>node --test tests/building-steering.test.js</automated>
  </verify>
  <done>The steering decision layer is deterministic, inspectable, and independent from Three.js scene objects.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate enemy steering into chase updates and stress-test blocked routes</name>
  <files>src/main.js, src/building-system.js, tests/building-steering.test.js</files>
  <behavior>
    - Enemy pursuit uses steering recommendations before boundary resolution.
    - Chases near blockers, funnels, pockets, and spawn-adjacent obstacles avoid persistent jitter or dead loops.
    - Sector/building interactions remain stable during long fixed-step stress runs.
  </behavior>
  <action>Wire building-aware steering into enemy update paths and add deterministic stress tests for `stress pursuit`, `corner squeeze`, `funnel chase`, and `spawn-near-building` scenarios.</action>
  <verify>
    <automated>node --test tests/building-steering.test.js tests/world-sectors.test.js</automated>
    <automated>npm run build</automated>
  </verify>
  <done>`BLD-03` is covered by explicit steering stability evidence instead of manual chase observation.</done>
</task>

</tasks>

<verification>
- [ ] Enemy steering is deterministic and free of ad-hoc randomness.
- [ ] Stress pursuit cases avoid persistent stuck loops and penetration.
- [ ] Sector traversal remains stable while steering around buildings.
</verification>

<success_criteria>
- All tasks completed.
- Enemy pursuit around tactical buildings is stable under repeatable fixed-step scenarios.
- No navmesh, heavy pathfinding, or asymmetric pass rules are introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/07-building-tactical-layer/07-03-SUMMARY.md`
</output>
