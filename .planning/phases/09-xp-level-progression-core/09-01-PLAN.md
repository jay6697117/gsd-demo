---
phase: 09-xp-level-progression-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/progression-config.js
  - src/progression-system.js
  - tests/progression-system.test.js
autonomous: true
requirements: [PROG-02]
user_setup: []
must_haves:
  truths:
    - Explicit XP values and level thresholds exist as a deterministic progression contract independent from score.
    - Level calculation and threshold-window helpers return stable current-level and next-level boundaries.
    - A single XP gain that crosses multiple thresholds emits pending level-up events in threshold order.
  artifacts:
    - src/progression-config.js
    - src/progression-system.js
    - tests/progression-system.test.js
  key_links:
    - progression config feeds level-window and XP gain helpers from one canonical data source.
    - pending level-up events are derived from threshold crossings rather than UI-side bookkeeping.
---

<objective>
Deliver the deterministic progression config and pure level-threshold logic for Phase 09.

Purpose: satisfy the structural half of `PROG-02` before runtime kill wiring or HUD work begins.
Output: explicit XP values, level-threshold contract, level-window helpers, and pure pending-event generation logic.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-xp-level-progression-core/09-CONTEXT.md
@.planning/phases/09-xp-level-progression-core/09-RESEARCH.md
@.planning/phases/09-xp-level-progression-core/09-VALIDATION.md
@src/main.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define deterministic XP values and level-threshold configuration</name>
  <files>src/progression-config.js, src/progression-system.js, tests/progression-system.test.js</files>
  <behavior>
    - XP values are defined independently from score for each enemy kind.
    - Level thresholds are explicit cumulative breakpoints with a documented overflow rule.
    - Helper APIs expose current level start and next-level boundary from total XP.
  </behavior>
  <action>Create a pure progression config plus helper layer that declares XP values by enemy kind, explicit threshold breakpoints, and stable helper functions for level lookup and level-window calculation.</action>
  <verify>
    <automated>node --test tests/progression-system.test.js</automated>
  </verify>
  <done>`PROG-02` has an explicit deterministic level curve contract that is decoupled from score and runtime side effects.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve XP gains into stable pending level-up events</name>
  <files>src/progression-system.js, src/progression-config.js, tests/progression-system.test.js</files>
  <behavior>
    - XP gains return updated total XP, resolved level, and ordered pending level-up events.
    - A single XP gain that crosses multiple thresholds produces one event per threshold in ascending order.
    - Event ids are derived from deterministic `eventSeq` progression.
  </behavior>
  <action>Implement pure progression reducers that consume XP gains, compute threshold crossings, update `eventSeq`, and emit stable `{ id, reachedLevel, thresholdXp }` queue entries without touching runtime UI or mode state.</action>
  <verify>
    <automated>node --test tests/progression-system.test.js</automated>
    <automated>npm run build</automated>
  </verify>
  <done>Wave 1 finishes with a deterministic progression reducer that can be safely consumed by the runtime kill pipeline.</done>
</task>

</tasks>

<verification>
- [ ] XP values are configured independently from score.
- [ ] Level-window helpers expose stable current and next thresholds.
- [ ] Multi-threshold XP gains emit ordered pending level-up events with stable ids.
</verification>

<success_criteria>
- All tasks completed.
- `PROG-02` is satisfied at the pure-rule level.
- No HUD, pause, or choice-panel semantics are introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/09-xp-level-progression-core/09-01-SUMMARY.md`
</output>
