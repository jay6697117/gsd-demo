---
phase: 09-xp-level-progression-core
plan: 04
type: execute
wave: 4
depends_on: [09-03]
files_modified:
  - src/main.js
  - src/progression-system.js
  - tests/progression-system.test.js
  - tests/determinism-contract.test.js
  - tests/playwright-progression-levels.test.js
autonomous: true
requirements: [PROG-05]
user_setup: []
must_haves:
  truths:
    - Restart and new-run flows fully reset level, XP, pending level-up events, and event sequencing.
    - A real browser route can prove kill -> XP -> first level-up cue under deterministic conditions.
    - Phase 09 regression preserves Phase 06/07/08 traversal, building, loot, and equipment contracts.
  artifacts:
    - src/main.js
    - src/progression-system.js
    - tests/progression-system.test.js
    - tests/determinism-contract.test.js
    - tests/playwright-progression-levels.test.js
  key_links:
    - startRun rebuilds progression state from one canonical reset path rather than piecemeal cleanup.
    - browser progression evidence reads deterministic snapshot data to prove runtime parity instead of relying on HUD text alone.
---

<objective>
Deliver restart parity and full regression evidence for the Phase 09 progression core.

Purpose: satisfy `PROG-05` and prepare clean must-have evidence for `09-VERIFICATION.md`.
Output: progression reset parity, browser kill-to-level route, and cumulative regression safety across Phases 06-08.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-xp-level-progression-core/09-CONTEXT.md
@.planning/phases/09-xp-level-progression-core/09-RESEARCH.md
@.planning/phases/09-xp-level-progression-core/09-VALIDATION.md
@.planning/phases/09-xp-level-progression-core/09-03-PLAN.md
@src/main.js
@src/determinism-harness.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reset progression state deterministically on new run and restart</name>
  <files>src/main.js, src/progression-system.js, tests/progression-system.test.js, tests/determinism-contract.test.js</files>
  <behavior>
    - `startRun()` rebuilds progression state to initial values.
    - Restart clears `level`, `totalXp`, `pendingLevelUps`, and `eventSeq` with no leakage from prior runs.
    - Snapshot and pure tests can prove reset parity.
  </behavior>
  <action>Integrate progression reset into the canonical run-start path and extend pure plus contract tests so restart parity is validated against both runtime state and deterministic snapshot output.</action>
  <verify>
    <automated>node --test tests/progression-system.test.js tests/determinism-contract.test.js</automated>
    <automated>npm run build</automated>
  </verify>
  <done>`PROG-05` has a single deterministic reset path with explicit parity evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Add browser progression route and full regression gate</name>
  <files>tests/playwright-progression-levels.test.js, src/main.js, tests/determinism-contract.test.js</files>
  <behavior>
    - Browser route reaches at least the first level-up cue through real combat kills.
    - Browser assertions use `render_game_to_text()` to validate progression state, not DOM text alone.
    - Full regression continues to include traversal, building, loot, and equipment suites.
  </behavior>
  <action>Add a deterministic Playwright route that secures enough kills for the first level-up, asserts HUD cue plus `progressionState`, and runs the cumulative full regression command to confirm Phase 09 does not regress Phases 06-08.</action>
  <verify>
    <automated>node tests/playwright-progression-levels.test.js</automated>
    <automated>npm run build && node --test tests/world-sectors.test.js tests/building-system.test.js tests/building-tactics.test.js tests/building-steering.test.js tests/breakable-system.test.js tests/drop-system.test.js tests/equipment-system.test.js tests/progression-system.test.js tests/determinism-contract.test.js && node tests/playwright-breakables-loot.test.js && node tests/playwright-progression-levels.test.js</automated>
  </verify>
  <done>Wave 4 closes Phase 09 with restart parity, browser evidence, and a cumulative regression gate ready for phase verification.</done>
</task>

</tasks>

<verification>
- [ ] Restart/new run fully reset progression state with no queue leakage.
- [ ] Browser route proves kill -> XP -> level-up cue with deterministic snapshot assertions.
- [ ] Full regression keeps Phase 06/07/08 behavior green.
</verification>

<success_criteria>
- All tasks completed.
- `PROG-05` is satisfied with deterministic reset evidence.
- `09-VERIFICATION.md` can be written directly from browser, contract, and regression artifacts.
</success_criteria>

<output>
After completion, create `.planning/phases/09-xp-level-progression-core/09-04-SUMMARY.md`
</output>
