---
phase: 09-xp-level-progression-core
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - src/main.js
  - src/progression-system.js
  - src/progression-config.js
  - tests/progression-system.test.js
autonomous: true
requirements: [PROG-01, PROG-03]
user_setup: []
must_haves:
  truths:
    - Enemy kills are the only runtime source of XP gains.
    - Each threshold crossing emits exactly one pending level-up event in stable order.
    - Progression state is tracked explicitly as `level`, `totalXp`, `pendingLevelUps`, and `eventSeq` without introducing a new gameplay mode.
  artifacts:
    - src/main.js
    - src/progression-system.js
    - src/progression-config.js
    - tests/progression-system.test.js
  key_links:
    - enemy death resolution feeds progression gain through one canonical runtime path.
    - progression event queue is derived from pure reducers instead of UI-side or banner-side state.
---

<objective>
Deliver canonical kill-driven XP intake and deterministic pending level-up event emission.

Purpose: satisfy `PROG-01` and `PROG-03` before HUD and snapshot surfaces are added.
Output: runtime progression state, kill-pipeline XP gain integration, and stable queued level-up events.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-xp-level-progression-core/09-CONTEXT.md
@.planning/phases/09-xp-level-progression-core/09-RESEARCH.md
@.planning/phases/09-xp-level-progression-core/09-VALIDATION.md
@.planning/phases/09-xp-level-progression-core/09-01-PLAN.md
@src/main.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire progression state into the canonical enemy-death pipeline</name>
  <files>src/main.js, src/progression-system.js, src/progression-config.js, tests/progression-system.test.js</files>
  <behavior>
    - Runtime state includes `state.progression = { level, totalXp, pendingLevelUps, eventSeq }`.
    - XP is awarded only when enemies actually die in the canonical kill-resolution loop.
    - Score and XP remain independent values.
  </behavior>
  <action>Initialize progression state in runtime and connect XP gains to the existing enemy-death resolution path so every killed enemy awards progression using the configured XP table without mutating score-driven behavior.</action>
  <verify>
    <automated>node --test tests/progression-system.test.js</automated>
    <automated>npm run build</automated>
  </verify>
  <done>`PROG-01` has a deterministic kill-to-XP runtime path with no alternate write channel.</done>
</task>

<task type="auto">
  <name>Task 2: Emit stable pending level-up events for every threshold crossing</name>
  <files>src/main.js, src/progression-system.js, tests/progression-system.test.js</files>
  <behavior>
    - Each threshold crossing produces exactly one pending level-up event.
    - Multi-threshold gains preserve ascending threshold order and incrementing event ids.
    - Runtime stays in existing play flow; no new `levelup` mode, pause state, or choice panel is introduced.
  </behavior>
  <action>Apply the pure progression reducer inside runtime kill handling so threshold crossings update level, queue deterministic pending events, and keep combat flow running without modal or pause semantics.</action>
  <verify>
    <automated>node --test tests/progression-system.test.js</automated>
    <automated>npm run build</automated>
  </verify>
  <done>`PROG-03` is satisfied with stable event sequencing that Phase 10 can consume later.</done>
</task>

</tasks>

<verification>
- [ ] XP only enters progression state from enemy kills.
- [ ] Each threshold crossing emits exactly one queued event.
- [ ] No new gameplay mode or choice-panel behavior is introduced.
</verification>

<success_criteria>
- All tasks completed.
- `PROG-01` and `PROG-03` are satisfied through one runtime kill pipeline.
- Score remains independent from XP and level progression.
</success_criteria>

<output>
After completion, create `.planning/phases/09-xp-level-progression-core/09-02-SUMMARY.md`
</output>
