---
phase: 09-xp-level-progression-core
plan: 03
type: execute
wave: 3
depends_on: [09-02]
files_modified:
  - src/main.js
  - src/determinism-harness.js
  - tests/progression-system.test.js
  - tests/determinism-contract.test.js
autonomous: true
requirements: [PROG-04]
user_setup: []
must_haves:
  truths:
    - HUD continuously shows current level and XP progression during combat.
    - Level-up arrival is surfaced through a non-blocking cue that does not change `state.mode`.
    - `progressionState` is serialized through the deterministic snapshot with stable fields and ordering.
  artifacts:
    - src/main.js
    - src/determinism-harness.js
    - tests/progression-system.test.js
    - tests/determinism-contract.test.js
  key_links:
    - runtime progression state feeds both HUD output and snapshot export from the same canonical data.
    - level-up cues are derived from queued events without introducing pause semantics or UI-only state.
---

<objective>
Deliver a persistent HUD progression surface and deterministic snapshot bridge for Phase 09.

Purpose: satisfy `PROG-04` while keeping level-up cues non-blocking and replay-explainable.
Output: HUD `Lvl` / `XP` display, lightweight `LEVEL UP` cue, and `progressionState` snapshot coverage.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-xp-level-progression-core/09-CONTEXT.md
@.planning/phases/09-xp-level-progression-core/09-RESEARCH.md
@.planning/phases/09-xp-level-progression-core/09-VALIDATION.md
@.planning/phases/09-xp-level-progression-core/09-02-PLAN.md
@src/main.js
@src/determinism-harness.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Surface level and XP progress through the combat HUD and feedback cues</name>
  <files>src/main.js, tests/progression-system.test.js</files>
  <behavior>
    - HUD continuously exposes `Lvl` and current/next XP information during active combat.
    - Level-up arrival triggers a non-blocking `LEVEL UP` cue using existing feedback/banner channels.
    - Combat remains in its current mode and continues to advance while cues are shown.
  </behavior>
  <action>Extend the existing HUD and feedback flow so progression state becomes visible in combat, including a lightweight level-up cue that reuses current banner semantics without introducing a new mode or modal panel.</action>
  <verify>
    <automated>node --test tests/progression-system.test.js tests/determinism-contract.test.js</automated>
  </verify>
  <done>`PROG-04` is visible to players without changing the battle-control contract.</done>
</task>

<task type="auto">
  <name>Task 2: Serialize deterministic progressionState for replay and assertion workflows</name>
  <files>src/determinism-harness.js, src/main.js, tests/determinism-contract.test.js, tests/progression-system.test.js</files>
  <behavior>
    - Snapshot exports `level`, `totalXp`, current and next threshold bounds, pending queue count, queue contents, and `eventSeq`.
    - Snapshot fields are stable-order and sufficient to explain why a level-up cue occurred.
    - `window.advanceTime(ms)` contract remains unchanged.
  </behavior>
  <action>Add `progressionState` to the deterministic snapshot contract and extend contract tests so replay tooling can assert level progression, pending-event queue contents, and threshold windows without reading DOM-only state.</action>
  <verify>
    <automated>node --test tests/progression-system.test.js tests/determinism-contract.test.js</automated>
    <automated>npm run build</automated>
  </verify>
  <done>Phase 09 now exposes machine-readable progression evidence that browser regression can consume in Wave 4.</done>
</task>

</tasks>

<verification>
- [ ] HUD continuously shows level and XP progression.
- [ ] Level-up cues are visible but non-blocking.
- [ ] `progressionState` is exported through the deterministic snapshot.
</verification>

<success_criteria>
- All tasks completed.
- `PROG-04` is satisfied without new pause or level-up modes.
- Snapshot contract is stable enough for browser and verifier consumption.
</success_criteria>

<output>
After completion, create `.planning/phases/09-xp-level-progression-core/09-03-SUMMARY.md`
</output>
