---
phase: 10-skill-talent-choice-engine
plan: 03
type: execute
wave: 3
depends_on: [10-02]
files_modified:
  - src/main.js
  - src/levelup-system.js
  - src/determinism-harness.js
  - tests/levelup-system.test.js
  - tests/determinism-contract.test.js
autonomous: true
requirements: [TAL-05]
user_setup: []
must_haves:
  truths:
    - Selecting an upgrade immediately changes measurable combat behavior or player stats.
    - Applied upgrades are stored separately from the transient offer panel state.
    - Snapshot output can explain both current panel state and applied upgrade effects.
  artifacts:
    - src/main.js
    - src/levelup-system.js
    - src/determinism-harness.js
    - tests/levelup-system.test.js
    - tests/determinism-contract.test.js
  key_links:
    - confirmed choices update runtime modifiers through one canonical apply-upgrade path.
    - snapshot exposes level-up offer state and applied upgrade state for deterministic assertions.
---

<objective>
Deliver immediate upgrade application and deterministic observability for the choice engine.

Purpose: satisfy `TAL-05` and prepare Phase 10 for browser verification and reroll hardening.
Output: applied skill/talent effects, derived modifier plumbing, and snapshot-visible `levelUpState` / `upgradeState`.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-skill-talent-choice-engine/10-CONTEXT.md
@.planning/phases/10-skill-talent-choice-engine/10-RESEARCH.md
@.planning/phases/10-skill-talent-choice-engine/10-VALIDATION.md
@.planning/phases/10-skill-talent-choice-engine/10-02-PLAN.md
@src/main.js
@src/determinism-harness.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply selected skill/talent effects immediately through runtime modifiers</name>
  <files>src/main.js, src/levelup-system.js, tests/levelup-system.test.js</files>
  <behavior>
    - Chosen upgrades become part of persistent run-local upgrade state.
    - Effects immediately change measurable combat parameters or player stats.
    - Base tuning constants remain immutable; upgrades flow through explicit modifiers / derived helpers.
  </behavior>
  <action>Implement an apply-upgrade path that stores chosen upgrades, recomputes combat-relevant modifiers, and makes the effect visible in the same run without mutating base constants.</action>
  <verify>
    <automated>node --test tests/levelup-system.test.js</automated>
  </verify>
  <done>`TAL-05` has immediate runtime impact rather than delayed or cosmetic-only application.</done>
</task>

<task type="auto">
  <name>Task 2: Extend deterministic snapshot with level-up and upgrade state</name>
  <files>src/determinism-harness.js, src/levelup-system.js, tests/determinism-contract.test.js, tests/levelup-system.test.js</files>
  <behavior>
    - Snapshot includes current offer/panel state and applied upgrade state.
    - Output order is stable and machine-readable for browser assertions.
    - Schema version is explicitly bumped for the new external contract.
  </behavior>
  <action>Serialize `levelUpState` and `upgradeState` through the determinism harness so `activeEventId`, `currentOfferId`, `offeredChoices`, `selectedIndex`, `rerollsRemaining`, `offerSeq`, `offerRngState`, `appliedChoices`, `skillModifiers`, and `talentModifiers` are all explainable from text state.</action>
  <verify>
    <automated>node --test tests/levelup-system.test.js tests/determinism-contract.test.js</automated>
    <automated>npm run build</automated>
  </verify>
  <done>Phase 10 now has deterministic text-state evidence for chosen upgrades and the active choice panel.</done>
</task>

</tasks>

<verification>
- [ ] Selected upgrades immediately change measurable runtime behavior.
- [ ] Applied upgrades remain separate from transient panel state.
- [ ] Snapshot exports stable `levelUpState.activeEventId/currentOfferId/offeredChoices/selectedIndex/rerollsRemaining/offerSeq/offerRngState`.
- [ ] Snapshot exports stable `upgradeState.appliedChoices/skillModifiers/talentModifiers`.
</verification>

<success_criteria>
- All tasks completed.
- `TAL-05` is satisfied with runtime and snapshot evidence.
- Offer/upgrade observability is ready for browser assertions.
</success_criteria>

<output>
After completion, create `.planning/phases/10-skill-talent-choice-engine/10-03-SUMMARY.md`
</output>
