---
phase: 10-skill-talent-choice-engine
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - src/main.js
  - src/style.css
  - src/levelup-system.js
  - tests/levelup-system.test.js
autonomous: true
requirements: [TAL-01, TAL-02]
user_setup: []
must_haves:
  truths:
    - A pending level-up event opens a dedicated `levelup_choice` mode with exactly 3 visible options.
    - The user can move focus and confirm exactly one option, after which combat resumes cleanly.
    - The modal flow does not introduce input lock or break existing compare/restart/fullscreen semantics.
  artifacts:
    - src/main.js
    - src/style.css
    - src/levelup-system.js
    - tests/levelup-system.test.js
  key_links:
    - pending level-up queue head drives one offer cycle and one modal session.
    - confirming an offer resolves the modal and hands control back to `playing`.
---

<objective>
Deliver the runtime level-up choice mode and exactly-one-selection flow.

Purpose: satisfy `TAL-01` and `TAL-02` after pure offer generation is already stable.
Output: `levelup_choice` mode, 3-option panel, focus controls, confirm path, and clean resume to combat.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-skill-talent-choice-engine/10-CONTEXT.md
@.planning/phases/10-skill-talent-choice-engine/10-RESEARCH.md
@.planning/phases/10-skill-talent-choice-engine/10-VALIDATION.md
@.planning/phases/10-skill-talent-choice-engine/10-01-PLAN.md
@src/main.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enter deterministic `levelup_choice` mode from the pending event queue</name>
  <files>src/main.js, src/levelup-system.js, tests/levelup-system.test.js</files>
  <behavior>
    - Queue head opens one level-up session at a time.
    - Combat, enemy updates, spawn, and auto pickup pause while the panel is active.
    - Existing `equip_compare` precedence remains deterministic and conflict-free.
  </behavior>
  <action>Wire pending level-up events into a dedicated runtime mode that opens from gameplay, freezes combat progression, and blocks new pickup/compare entries until the current choice resolves.</action>
  <verify>
    <automated>node --test tests/levelup-system.test.js</automated>
  </verify>
  <done>`TAL-01` has a deterministic modal entry path fed by the progression queue.</done>
</task>

<task type="auto">
  <name>Task 2: Render 3 choices and confirm exactly one without input lock</name>
  <files>src/main.js, src/style.css, src/levelup-system.js, tests/levelup-system.test.js</files>
  <behavior>
    - Panel visibly shows 3 choices and current focus.
    - `ArrowLeft` / `KeyA` and `ArrowRight` / `KeyD` move focus.
    - `Enter` / `Space` confirms one option, consumes the active event, and returns to `playing`.
  </behavior>
  <action>Implement the choice panel UI and input handling so one offer is selected per event and gameplay resumes without stuck input or broken control semantics.</action>
  <verify>
    <automated>node --test tests/levelup-system.test.js</automated>
    <automated>npm run build</automated>
  </verify>
  <done>`TAL-02` is satisfied with a stable exactly-one-selection loop.</done>
</task>

</tasks>

<verification>
- [ ] A pending level-up event opens exactly 3 visible choices.
- [ ] Focus movement and confirm work deterministically.
- [ ] Choosing one option returns cleanly to combat without input lock.
</verification>

<success_criteria>
- All tasks completed.
- `TAL-01` and `TAL-02` are satisfied.
- No reroll or effect-application complexity leaks into this wave beyond selection flow.
</success_criteria>

<output>
After completion, create `.planning/phases/10-skill-talent-choice-engine/10-02-SUMMARY.md`
</output>

