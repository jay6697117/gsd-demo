---
phase: 10-skill-talent-choice-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/upgrade-catalog.js
  - src/levelup-system.js
  - tests/levelup-system.test.js
autonomous: true
requirements: [TAL-03, TAL-04]
user_setup: []
must_haves:
  truths:
    - A deterministic catalog supports both `skill` and `talent` choices with explicit eligibility/exclusion rules.
    - Offer generation returns exactly 3 unique eligible options for a level-up event.
    - Invalid or duplicate options are filtered in pure logic before any UI/runtime rendering.
  artifacts:
    - src/upgrade-catalog.js
    - src/levelup-system.js
    - tests/levelup-system.test.js
  key_links:
    - pending level-up events can be transformed into offer candidates through one canonical deterministic generator.
    - offer filtering is driven by pure catalog constraints rather than panel-side ad-hoc cleanup.
---

<objective>
Deliver the deterministic upgrade catalog and pure offer-generation rules for Phase 10.

Purpose: satisfy the structural half of `TAL-03` and `TAL-04` before any modal flow or runtime panel logic is introduced.
Output: explicit skill/talent catalog, eligibility/exclusion rules, and a pure generator that emits exactly 3 legal choices.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-skill-talent-choice-engine/10-CONTEXT.md
@.planning/phases/10-skill-talent-choice-engine/10-RESEARCH.md
@.planning/phases/10-skill-talent-choice-engine/10-VALIDATION.md
@src/progression-system.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define a deterministic skill/talent catalog with explicit constraints</name>
  <files>src/upgrade-catalog.js, src/levelup-system.js, tests/levelup-system.test.js</files>
  <behavior>
    - Catalog contains both `skill` and `talent` entries.
    - Each entry declares enough metadata to evaluate eligibility, exclusion, and rank/pool constraints.
    - Catalog size and constraints guarantee at least 3 legal offers in the early run.
  </behavior>
  <action>Create an authored upgrade catalog plus pure helpers for reading constraints so the later offer generator does not need to infer validity from UI state.</action>
  <verify>
    <automated>node --test tests/levelup-system.test.js</automated>
  </verify>
  <done>`TAL-03` has a deterministic authored catalog contract instead of implicit choice lists.</done>
</task>

<task type="auto">
  <name>Task 2: Generate exactly 3 unique eligible offers from the pending level-up event</name>
  <files>src/levelup-system.js, src/upgrade-catalog.js, tests/levelup-system.test.js</files>
  <behavior>
    - Offer generation emits exactly 3 choices.
    - Duplicate, invalid, excluded, or maxed entries are filtered before selection completes.
    - Output order is deterministic for the same `offerRngState`, queue head, and upgrade state.
  </behavior>
  <action>Implement a pure deterministic offer generator that transforms the active level-up event and current upgrade state into 3 legal choices with stable ordering.</action>
  <verify>
    <automated>node --test tests/levelup-system.test.js</automated>
    <automated>npm run build</automated>
  </verify>
  <done>`TAL-04` is satisfied at the pure-rule layer with deterministic 3-offer output.</done>
</task>

</tasks>

<verification>
- [ ] Catalog supports both `skill` and `talent` entries.
- [ ] Offer generation emits exactly 3 deterministic unique eligible choices.
- [ ] Duplicate and invalid options are filtered in pure logic.
</verification>

<success_criteria>
- All tasks completed.
- `TAL-03` and `TAL-04` are satisfied at the catalog / offer-rule level.
- No runtime mode, panel, or reroll UI is introduced yet.
</success_criteria>

<output>
After completion, create `.planning/phases/10-skill-talent-choice-engine/10-01-SUMMARY.md`
</output>

