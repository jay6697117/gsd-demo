---
phase: 05-automation-determinism-harness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main.js
  - tests/determinism-contract.test.js
autonomous: true
requirements: [AUTO-01, AUTO-02]
user_setup: []
must_haves:
  truths:
    - render_game_to_text returns stable and assertion-ready gameplay contract fields.
    - advanceTime(ms) produces repeatable simulation state progression for equivalent step inputs.
    - deterministic contract behavior is covered by automated tests.
  artifacts:
    - src/main.js
    - tests/determinism-contract.test.js
  key_links:
    - render bridge output fields are consumed by deterministic contract tests.
    - advanceTime fixed-step semantics are validated against repeat-run state snapshots.
---

<objective>
Lock deterministic runtime contracts for automation by hardening `render_game_to_text` and `advanceTime` behavior.

Purpose: convert existing debug bridge into stable, test-backed assertions input.
Output: deterministic state payload contract and repeatable time-step tests.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-automation-determinism-harness/05-RESEARCH.md
@.planning/phases/05-automation-determinism-harness/05-VALIDATION.md
@src/main.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stabilize render_game_to_text contract schema for assertions</name>
  <files>src/main.js, tests/determinism-contract.test.js</files>
  <behavior>
    - Payload includes deterministic gameplay-relevant fields only.
    - Key sections are explicitly present and type-stable across runs.
    - Contract tests fail when required fields drift or disappear.
  </behavior>
  <action>Normalize `render_game_to_text` output contract (including mode/time/player/enemy and deterministic support metadata), then add Node contract tests that parse and validate schema presence, type stability, and deterministic formatting assumptions.</action>
  <verify>
    <automated>node --test tests/determinism-contract.test.js</automated>
  </verify>
  <done>AUTO-01 is machine-verifiable through deterministic schema tests.</done>
</task>

<task type="auto">
  <name>Task 2: Harden advanceTime deterministic stepping semantics</name>
  <files>src/main.js, tests/determinism-contract.test.js</files>
  <behavior>
    - Equivalent step inputs produce repeatable state transitions from same seed/run start.
    - advanceTime path uses fixed-step deterministic loop semantics.
    - Contract tests validate deterministic progression and guard against drift regressions.
  </behavior>
  <action>Refine `window.advanceTime(ms)` behavior as deterministic simulation adapter and extend contract tests to compare repeat-run snapshots under identical time-step sequences.</action>
  <verify>
    <automated>node --test tests/determinism-contract.test.js</automated>
    <automated>npm run build</automated>
  </verify>
  <done>AUTO-02 is covered with deterministic stepping assertions and build-stable integration.</done>
</task>

</tasks>

<verification>
- [ ] Determinism contract tests pass.
- [ ] Build succeeds.
- [ ] AUTO-01 and AUTO-02 map to explicit machine assertions.
</verification>

<success_criteria>
- All tasks completed.
- Runtime bridge contract is stable and deterministic.
- Time-stepping is repeatable for equivalent inputs.
</success_criteria>

<output>
After completion, create `.planning/phases/05-automation-determinism-harness/05-01-SUMMARY.md`
</output>
