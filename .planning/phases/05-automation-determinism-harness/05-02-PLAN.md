---
phase: 05-automation-determinism-harness
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - tests/playwright-burst.test.js
  - package.json
  - src/main.js
autonomous: true
requirements: [AUTO-03]
user_setup: []
must_haves:
  truths:
    - Automated Playwright burst actions can drive gameplay without runtime failures.
    - Burst run emits reproducible artifacts (screenshot + state JSON) for audit.
    - Burst harness checks console/runtime errors and fails on critical issues.
  artifacts:
    - tests/playwright-burst.test.js
    - package.json
  key_links:
    - burst runner consumes render_game_to_text output after scripted inputs.
    - burst execution writes artifacts and asserts runtime health from browser signals.
---

<objective>
Implement browser-level burst automation harness and artifact flow for Phase 5.

Purpose: prove end-to-end machine-driven gameplay progression with deterministic artifacts.
Output: runnable Playwright burst test script, artifact outputs, and runtime-error guardrails.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-automation-determinism-harness/05-RESEARCH.md
@.planning/phases/05-automation-determinism-harness/05-VALIDATION.md
@.planning/phases/05-automation-determinism-harness/05-01-SUMMARY.md
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Playwright burst runner with artifact emission</name>
  <files>tests/playwright-burst.test.js, package.json</files>
  <behavior>
    - Script launches app page and executes short input bursts (move/attack/pause/fullscreen toggles as needed).
    - Script captures screenshot and render_game_to_text JSON to stable artifact path.
    - Script exits non-zero on runtime/console critical errors.
  </behavior>
  <action>Create Playwright burst harness script and wire runnable command so AUTO-03 can be executed repeatedly with deterministic artifact output names/paths.</action>
  <verify>
    <automated>node tests/playwright-burst.test.js</automated>
  </verify>
  <done>Burst harness reliably produces artifacts and basic runtime-health checks.</done>
</task>

<task type="auto">
  <name>Task 2: Harden burst assertions and integrate regression command path</name>
  <files>tests/playwright-burst.test.js, package.json, src/main.js</files>
  <behavior>
    - Burst run validates gameplay progression (state changes after action burst).
    - Console/runtime error filtering is explicit and deterministic.
    - Command path is documented in npm scripts for repeatable regression runs.
  </behavior>
  <action>Expand burst runner assertions to enforce gameplay progression and no-critical-error guarantees, then integrate command aliases and minor runtime hooks needed for stable automated capture.</action>
  <verify>
    <automated>node tests/playwright-burst.test.js</automated>
    <automated>npm run build</automated>
  </verify>
  <done>AUTO-03 is fully covered by repeatable burst automation with artifact-backed verification.</done>
</task>

</tasks>

<verification>
- [ ] Playwright burst script runs end-to-end without critical runtime errors.
- [ ] Screenshot and JSON artifacts are generated.
- [ ] Build succeeds after harness integration.
</verification>

<success_criteria>
- All tasks completed.
- AUTO-03 is machine-verifiable via artifact-backed Playwright bursts.
- Harness is repeatable and regression-friendly.
</success_criteria>

<output>
After completion, create `.planning/phases/05-automation-determinism-harness/05-02-SUMMARY.md`
</output>
