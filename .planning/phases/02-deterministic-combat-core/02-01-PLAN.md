---
phase: 02-deterministic-combat-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main.js
autonomous: true
requirements: [COMB-01, COMB-02, COMB-03]
user_setup: []
must_haves:
  truths:
    - Player movement responds reliably in four directions.
    - Primary attack trigger and cooldown behavior are deterministic.
    - Hit detection results are reproducible for equivalent input/time slices.
  artifacts:
    - src/main.js
  key_links:
    - input sampling drives movement and attack through fixed-step updates.
    - attack execution updates enemy health through deterministic geometric checks.
---

<objective>
Implement deterministic player-control and hit-resolution foundations for Phase 2.

Purpose: establish stable combat mechanics before introducing full pressure and closure logic.
Output: reliable movement, attack trigger/cooldown, and deterministic hit processing.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-deterministic-combat-core/02-CONTEXT.md
@.planning/phases/02-deterministic-combat-core/02-RESEARCH.md
@src/main.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden deterministic input and movement step behavior</name>
  <files>src/main.js</files>
  <behavior>
    - Four-direction input remains responsive under sustained play.
    - Edge-trigger and sustained input channels stay role-separated.
  </behavior>
  <action>Refine `applyPlayerInput` and input state handling to preserve deterministic movement response and avoid stale-state drift across fixed-step updates.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Movement behavior is deterministic and COMB-01 is explicitly satisfiable.</done>
</task>

<task type="auto">
  <name>Task 2: Stabilize attack trigger, cooldown, and hit detection path</name>
  <files>src/main.js</files>
  <behavior>
    - Attack only triggers on expected edge events.
    - Cooldown enforcement is stable.
    - Forward-sector hit detection remains deterministic.
  </behavior>
  <action>Harden `doAttack` and related cooldown logic to ensure deterministic hit decisions, consistent enemy damage application, and reproducible outcomes for repeated time/input sequences.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>COMB-02 and COMB-03 are covered with explicit deterministic behavior criteria.</done>
</task>

</tasks>

<verification>
- [ ] Build succeeds.
- [ ] Input and attack logic paths remain fixed-step deterministic.
- [ ] Requirement mappings COMB-01/02/03 remain explicit.
</verification>

<success_criteria>
- All tasks completed.
- Control and hit mechanics are deterministic and reproducible.
</success_criteria>

<output>
After completion, create `.planning/phases/02-deterministic-combat-core/02-01-SUMMARY.md`
</output>
