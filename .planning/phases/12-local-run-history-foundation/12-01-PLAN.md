---
phase: 12-local-run-history-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/meta-history.js
  - src/main.js
  - tests/meta-history.test.js
autonomous: true
requirements: [META-01]
user_setup: []
must_haves:
  truths:
    - Completed standard runs can be normalized into one stable local summary model.
    - Best and recent history are bounded separately with deterministic ordering.
    - History persistence is versioned and standard-only before any UI work begins.
  artifacts:
    - src/meta-history.js
    - tests/meta-history.test.js
    - src/main.js
  key_links:
    - the game-over write boundary produces normalized records through one canonical summary builder.
    - persisted history reads and writes use one versioned adapter instead of ad-hoc localStorage calls.
---

<objective>
Build the standard-run history model, ranking rules, and local persistence contract.

Purpose: satisfy the data and storage half of `META-01` before Phase 12 touches any overlay UI.
Output: normalized standard-run records, deterministic `bestRuns` / `recentRuns`, bounded retention, and a versioned persistence adapter.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-local-run-history-foundation/12-CONTEXT.md
@.planning/phases/12-local-run-history-foundation/12-RESEARCH.md
@.planning/phases/12-local-run-history-foundation/12-VALIDATION.md
@src/main.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the normalized standard-run record and deterministic history rules</name>
  <files>src/meta-history.js, tests/meta-history.test.js</files>
  <behavior>
    - A completed standard run is normalized into one stable record with `id`, `mode`, `score`, `time`, `kills`, `level`, `seed`, and `playedAt`.
    - Ranking uses `Score -> Time -> Kills -> Level` with stable fallback ordering.
    - `bestRuns` and `recentRuns` are maintained as separate bounded lists with limits `10` and `20`.
    - Non-standard modes are ignored by the standard history reducer.
  </behavior>
  <action>Implement a pure meta-history rule layer that builds one canonical standard-run record, updates best/recent projections deterministically, and locks the ranking/retention contract in pure tests.</action>
  <verify>
    <automated>node --test tests/meta-history.test.js</automated>
  </verify>
  <done>`META-01` has a deterministic summary and ranking core instead of ad-hoc UI-side sorting.</done>
</task>

<task type="auto">
  <name>Task 2: Add a versioned local persistence adapter and runtime hydration boundary</name>
  <files>src/meta-history.js, src/main.js, tests/meta-history.test.js</files>
  <behavior>
    - Local persistence uses one explicit versioned shape: `{ version, bestRuns, recentRuns }`.
    - Runtime hydration occurs through the canonical start/run initialization path rather than overlay rendering.
    - No duplicate writes occur from restart, start-screen rendering, or snapshot generation.
  </behavior>
  <action>Introduce a narrow local history storage adapter and wire runtime hydration to the start/run lifecycle so persisted standard history is available before UI rendering without creating extra write paths.</action>
  <verify>
    <automated>node --test tests/meta-history.test.js</automated>
    <automated>npm run build</automated>
  </verify>
  <done>Standard history persistence exists as a stable contract that Phase 12 UI work can consume safely.</done>
</task>

</tasks>

<verification>
- [ ] Standard run records have the required stable fields.
- [ ] `bestRuns` and `recentRuns` use deterministic ordering and bounded retention.
- [ ] Standard history persistence is versioned and standard-only.
- [ ] Runtime hydration does not introduce duplicate history writes.
</verification>

<success_criteria>
- All tasks completed.
- `META-01` data/storage foundation is satisfied.
- No Phase 12 UI work or daily challenge semantics are introduced yet.
</success_criteria>

<output>
After completion, create `.planning/phases/12-local-run-history-foundation/12-01-SUMMARY.md`
</output>
