---
phase: 12-local-run-history-foundation
plan: 03
type: execute
wave: 3
depends_on: [12-02]
files_modified:
  - src/determinism-harness.js
  - tests/meta-history.test.js
  - tests/playwright-meta-history.test.js
  - tests/playwright-restart-parity.test.js
autonomous: true
requirements: [META-01, META-02]
user_setup: []
must_haves:
  truths:
    - Local standard history survives reload and browser restart while run-local state reset semantics stay intact.
    - Snapshot output exposes `metaState` as the machine-readable source of truth for local history.
    - Phase 12 browser evidence proves both overlay visibility and persistence without leaking Phase 13 daily fields.
  artifacts:
    - src/determinism-harness.js
    - tests/meta-history.test.js
    - tests/playwright-meta-history.test.js
    - tests/playwright-restart-parity.test.js
  key_links:
    - runtime meta history is serialized through the same deterministic snapshot bridge used by the rest of the game.
    - browser routes prove persistence and visibility through real reload/restart flows rather than privileged hooks.
---

<objective>
Freeze observability and browser proof for local run history.

Purpose: finish `META-01` and `META-02` with deterministic text-state evidence, reload persistence coverage, and regression protection against restart parity drift.
Output: `metaState` in the snapshot, one Playwright history route, and parity-safe regression evidence.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-local-run-history-foundation/12-CONTEXT.md
@.planning/phases/12-local-run-history-foundation/12-RESEARCH.md
@.planning/phases/12-local-run-history-foundation/12-VALIDATION.md
@.planning/phases/12-local-run-history-foundation/12-02-PLAN.md
@src/determinism-harness.js
@tests/helpers/playwright-game.js
@tests/playwright-restart-parity.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend deterministic snapshot with `metaState` for standard history assertions</name>
  <files>src/determinism-harness.js, tests/meta-history.test.js</files>
  <behavior>
    - `render_game_to_text()` exposes `metaState.bestRuns`, `metaState.recentRuns`, and `metaState.isExpanded`.
    - Snapshot output is stable and does not depend on direct storage inspection.
    - No daily challenge identity or Phase 13-only fields are added.
  </behavior>
  <action>Serialize standard history through the determinism harness so meta assertions use the same text-state contract as traversal, loot, progression, and level-up automation.</action>
  <verify>
    <automated>node --test tests/meta-history.test.js</automated>
  </verify>
  <done>Phase 12 history is observable and diagnosable through one canonical snapshot interface.</done>
</task>

<task type="auto">
  <name>Task 2: Add a browser route for gameover write, reload persistence, and dual-surface visibility</name>
  <files>tests/playwright-meta-history.test.js, tests/playwright-restart-parity.test.js</files>
  <behavior>
    - Browser route completes a standard run, verifies history write, reloads or restarts the browser session, and reads the history back.
    - Assertions prove both start-screen and gameover-screen visibility.
    - Existing restart parity semantics remain intact and are not regressed by the new persistence layer.
  </behavior>
  <action>Create a dedicated Playwright history route that proves local persistence survives reload/browser restart, validates both allowed overlay surfaces, and confirms history does not break existing run reset behavior.</action>
  <verify>
    <automated>node tests/playwright-meta-history.test.js</automated>
    <automated>npm run build && node --test tests/world-sectors.test.js tests/building-system.test.js tests/building-tactics.test.js tests/building-steering.test.js tests/breakable-system.test.js tests/drop-system.test.js tests/equipment-system.test.js tests/progression-system.test.js tests/levelup-system.test.js tests/meta-history.test.js tests/determinism-contract.test.js && node tests/playwright-burst.test.js && node tests/playwright-breakables-loot.test.js && node tests/playwright-levelup-choice.test.js && node tests/playwright-progression-levels.test.js && node tests/playwright-meta-history.test.js && node tests/playwright-restart-parity.test.js</automated>
  </verify>
  <done>Phase 12 has browser-level and regression-level proof for persistence, ranking visibility, and reset compatibility.</done>
</task>

</tasks>

<verification>
- [ ] Snapshot exports stable `metaState.bestRuns/recentRuns/isExpanded`.
- [ ] Browser route proves a completed standard run is persisted and readable after reload.
- [ ] Both start-screen and gameover-screen expose the history surface.
- [ ] Existing restart parity remains intact.
- [ ] No Phase 13 daily fields leak into runtime or snapshot state.
</verification>

<success_criteria>
- All tasks completed.
- `META-01` and `META-02` are satisfied with deterministic and browser-level evidence.
- Phase 12 is ready for `12-VERIFICATION.md` without reopening its scope.
</success_criteria>

<output>
After completion, create `.planning/phases/12-local-run-history-foundation/12-03-SUMMARY.md`
</output>
