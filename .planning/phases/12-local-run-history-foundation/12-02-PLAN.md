---
phase: 12-local-run-history-foundation
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - src/main.js
  - src/style.css
  - index.html
  - src/meta-history.js
autonomous: true
requirements: [META-01, META-02]
user_setup: []
must_haves:
  truths:
    - Standard history is written once at the real run-completion boundary and immediately visible on existing overlays.
    - Start-screen and gameover-screen present the same collapsed/expanded history structure.
    - Best and recent records are readable in compact 2-line form without introducing a new meta screen.
  artifacts:
    - src/main.js
    - src/style.css
    - index.html
    - src/meta-history.js
  key_links:
    - game-over completion writes persisted history through one canonical boundary.
    - both overlay surfaces render one shared history contract rather than divergent UI variants.
---

<objective>
Surface standard-run history on the existing start and gameover overlays.

Purpose: satisfy the visible half of `META-01` and `META-02` without expanding scope into daily challenge or standalone meta navigation.
Output: one collapsed history panel contract rendered on `start-screen` and `gameover-screen`, with `Best` above `Recent` when expanded.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-local-run-history-foundation/12-CONTEXT.md
@.planning/phases/12-local-run-history-foundation/12-RESEARCH.md
@.planning/phases/12-local-run-history-foundation/12-VALIDATION.md
@.planning/phases/12-local-run-history-foundation/12-01-PLAN.md
@index.html
@src/main.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write standard history at game-over and render a collapsed summary on both overlays</name>
  <files>src/main.js, index.html, src/meta-history.js</files>
  <behavior>
    - History writes happen exactly once when a standard run enters the game-over summary path.
    - `start-screen` and `gameover-screen` both show a collapsed history summary and one expand control.
    - The overlay surface uses the same underlying history data model on both screens.
  </behavior>
  <action>Integrate history persistence into the canonical game-over boundary and mount one collapsed history surface onto the existing start/gameover overlays without introducing a third meta page.</action>
  <verify>
    <automated>node --test tests/meta-history.test.js</automated>
  </verify>
  <done>Completed standard runs become locally visible from both existing overlays through one shared render contract.</done>
</task>

<task type="auto">
  <name>Task 2: Implement expanded `Best` / `Recent` layout with compact 2-line record density</name>
  <files>src/main.js, src/style.css, index.html</files>
  <behavior>
    - Expanded layout always shows `Best` first and `Recent` below.
    - Each record uses compact 2-line density: line 1 emphasizes `score/time`; line 2 shows `kills/level/seed/playedAt`.
    - Only standard history is rendered; no daily labels, challenge identity, or tabbed meta navigation appear.
  </behavior>
  <action>Wire the expansion toggle and overlay formatting so the history panel stays collapsed by default, expands into deterministic `Best`/`Recent` sections, and preserves the locked information order and density.</action>
  <verify>
    <automated>node --test tests/meta-history.test.js</automated>
    <automated>npm run build</automated>
  </verify>
  <done>`META-02` is visible on the two allowed surfaces with the locked hierarchy and information density.</done>
</task>

</tasks>

<verification>
- [ ] History is written only at the standard run completion boundary.
- [ ] `start-screen` and `gameover-screen` both expose the same collapsed summary and expand affordance.
- [ ] Expanded layout is `Best` above `Recent`.
- [ ] Each record follows the compact 2-line density with the required fields.
- [ ] No daily challenge fields or separate meta screen are introduced.
</verification>

<success_criteria>
- All tasks completed.
- `META-01` and `META-02` are satisfied at the UI surface level.
- Phase 12 stays within existing overlay shells.
</success_criteria>

<output>
After completion, create `.planning/phases/12-local-run-history-foundation/12-02-SUMMARY.md`
</output>
